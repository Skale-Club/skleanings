---
phase: 20-calendar-timeline-structure-audit
plan: 02
type: execute
wave: 1
depends_on: [20-01]
files_modified:
  - client/src/components/admin/AppointmentsCalendarSection.tsx
  - .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md
autonomous: false
requirements: [CAL-FIX-01, CAL-FIX-02, CAL-FIX-04]
must_haves:
  truths:
    - "AppointmentsCalendarSection.tsx no longer rebuilds the `components` object on every render (per RBC issue #2588 — fixes EventComponent re-mount path)"
    - "AppointmentsCalendarSection.tsx no longer rebuilds the `resourceProps` object on every render when isByStaff is true"
    - "The redundant on-mount `setCurrentView(DEFAULT_CALENDAR_VIEW)` useEffect is gone (RESEARCH.md Pitfall 4 — eliminates double-render on mount)"
    - "Inline view-change handler is wrapped in useCallback (eliminates inline arrow re-creation per render)"
    - "TypeScript compiles cleanly after all edits (`npm run check` exits 0)"
    - "Production build succeeds after all edits (`npm run build` exits 0)"
    - "Post-Wave-1 measurements are recorded in 20-DIAGNOSIS.md § Post-Wave-1 with delta vs Baseline"
  artifacts:
    - path: "client/src/components/admin/AppointmentsCalendarSection.tsx"
      provides: "Memoized components, resourceProps, visibleStaffForResources; useCallback view handler; deleted redundant useEffect"
      contains: "useMemo"
      contains_also: "useCallback"
    - path: ".planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md"
      provides: "Post-Wave-1 measurement table filled with 9 rows"
      contains: "Post-Wave-1"
  key_links:
    - from: "useMemo on `calendarComponents`"
      to: "<DnDCalendar components={calendarComponents}>"
      via: "components prop reference stays stable across renders"
      pattern: "components=\\{calendarComponents\\}"
    - from: "useMemo on `resourceProps`"
      to: "<DnDCalendar {...resourceProps}>"
      via: "resourceProps reference stays stable across renders when isByStaff/visibleStaff unchanged"
      pattern: "\\{\\.\\.\\.resourceProps\\}"
    - from: "20-DIAGNOSIS.md § Post-Wave-1"
      to: "Plan 03 Strategy Decision"
      via: "Wave 2 reads Post-Wave-1 numbers to choose CSS strategy"
      pattern: "Post-Wave-1"
---

<objective>
Wave 1 — Apply the HIGH-confidence structural fixes from RESEARCH.md § Suspect Hot Spots (lines 600–616). These five edits are independently justified by RBC issue #2588 and Pitfall 4 — they are code-quality cleanups that don't change visual design but stabilize component identity (which RESEARCH.md identifies as the most-likely root cause of both alignment drift AND view-switch staleness). After the edits land, re-run the diagnostic measurement to attribute any alignment improvements to these fixes specifically.

Purpose: Per RESEARCH.md, the CSS strategy (Wave 2) is diagnosis-dependent. By stabilizing component identity FIRST, we can rule out re-mount-driven layout instability before deciding whether additional CSS changes are needed. If Wave 1 alone resolves the misalignment (Strategy A in RESEARCH.md), Plan 03 becomes a no-op CSS change; otherwise Plan 03 picks B/C/D based on the residual offset pattern.

Output:
- `client/src/components/admin/AppointmentsCalendarSection.tsx` with: memoized `calendarComponents`, memoized `resourceProps`, memoized `visibleStaffForResources`, deleted redundant `setCurrentView` useEffect, useCallback view handler
- `.planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md` § Post-Wave-1 filled with 9 measurement rows
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-calendar-timeline-structure-audit/20-CONTEXT.md
@.planning/phases/20-calendar-timeline-structure-audit/20-RESEARCH.md
@.planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md
@.planning/phases/20-calendar-timeline-structure-audit/20-01-SUMMARY.md
@client/src/components/admin/AppointmentsCalendarSection.tsx

<interfaces>
<!-- Key code shapes the executor needs. Extracted from AppointmentsCalendarSection.tsx. -->

Existing imports (top of file ~line 1–60): `useEffect`, `useMemo`, `useState` already imported from `react` (verify and add `useCallback` if missing).

Module-scope constants: `DEFAULT_CALENDAR_VIEW` (Views.DAY), `DEFAULT_SCROLL_TIME`, `Views` (from react-big-calendar). DnDCalendar HOC at line 146.

Current call sites to modify:
- Line 932–934: `useEffect(() => { setCurrentView(DEFAULT_CALENDAR_VIEW); }, []);` — DELETE
- Line 936: `const visibleStaffForResources = scopedStaffList.filter((s) => !hiddenStaff.has(s.id));` — wrap in useMemo
- Line 938–945: `const resourceProps = isByStaff ? { ... } : {};` — wrap in useMemo
- Line 1110–1113: inline arrow `(v: string) => { setCurrentView(v); if (v !== 'day') setIsByStaff(false); }` passed to `onView` — extract to `handleViewChange` with useCallback
- Line 1117–1130: `components={{ event: EventComponent, toolbar: (props) => <CalendarToolbar ... /> }}` — extract to `calendarComponents` with useMemo

Stable identifiers (no edit):
- `EventComponent` — module-scope component (line 288 per CONTEXT.md), already stable
- `CalendarToolbar` — module-scope component, already stable
- `handleByStaff` — does NOT exist yet; the inline arrow on line 1123–1126 is currently inside the toolbar JSX. RESEARCH.md recommends extracting to useCallback but this is bundled with the calendarComponents memoization (deps: `[isByStaff, handleByStaff, filterPopover]`).

Existing closure deps to preserve:
- `setCurrentView` and `setIsByStaff` are stable React setters — safe to omit from deps
- `filterPopover` (line 947) is JSX rebuilt fresh every render — accept this in calendarComponents deps for now (RESEARCH.md notes memoizing filterPopover separately is conditional; not in this plan's scope)
- `isByStaff` and `visibleStaffForResources` are values — must be in deps
- `scopedStaffList` and `hiddenStaff` are dependencies of `visibleStaffForResources`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Memoize visibleStaffForResources, resourceProps; delete redundant useEffect; useCallback handlers; memoize calendarComponents</name>
  <files>client/src/components/admin/AppointmentsCalendarSection.tsx</files>
  <read_first>
    - client/src/components/admin/AppointmentsCalendarSection.tsx (full file — but pay attention to lines 1–60 imports, 920–946 effect/derived state, 1093–1136 JSX call site)
    - .planning/phases/20-calendar-timeline-structure-audit/20-CONTEXT.md (D-02 narrow scope, D-11 audit on-mount setCurrentView)
    - .planning/phases/20-calendar-timeline-structure-audit/20-RESEARCH.md (§ Architecture Patterns 2 & 3, lines 178–234; § Suspect Hot Spots #1, #2, #6, lines 604–616; § Code Examples — Memoizing components and resourceProps, lines 344–392; § Anti-Patterns to Avoid, lines 251–258)
  </read_first>
  <action>
Apply 5 edits to `client/src/components/admin/AppointmentsCalendarSection.tsx`. Make them atomically (one commit) — they are interrelated cleanups; partial application risks introducing the very re-mount churn we're fixing.

**Edit 1 — Verify imports (top of file):** Ensure `useCallback`, `useMemo`, `useEffect` are all imported from `react`. If `useCallback` is missing, add it. Example:
```typescript
import { useCallback, useEffect, useMemo, useState } from 'react';
```
(Keep alphabetical order if file uses it; otherwise match existing convention.)

**Edit 2 — DELETE redundant on-mount useEffect (lines 932–934):**
Remove EXACTLY these lines:
```typescript
useEffect(() => {
  setCurrentView(DEFAULT_CALENDAR_VIEW);
}, []);
```
Justification: RESEARCH.md Pitfall 4 — `useState(DEFAULT_CALENDAR_VIEW)` at line 397 already initializes the value; the useEffect is a tautological reset that triggers a double-render on mount.

**Edit 3 — Memoize `visibleStaffForResources` (currently line 936):**
Replace:
```typescript
const visibleStaffForResources = scopedStaffList.filter((s) => !hiddenStaff.has(s.id));
```
With:
```typescript
const visibleStaffForResources = useMemo(
  () => scopedStaffList.filter((s) => !hiddenStaff.has(s.id)),
  [scopedStaffList, hiddenStaff],
);
```

**Edit 4 — Memoize `resourceProps` (currently lines 938–945):**
Replace:
```typescript
const resourceProps = isByStaff
  ? {
      resources: visibleStaffForResources,
      resourceIdAccessor: (resource: any) => resource.id,
      resourceTitleAccessor: (resource: any) => resource.firstName,
      resourceAccessor: (event: any) => event.staffMemberId,
    }
  : {};
```
With:
```typescript
const resourceProps = useMemo(
  () => isByStaff
    ? {
        resources: visibleStaffForResources,
        resourceIdAccessor: (resource: any) => resource.id,
        resourceTitleAccessor: (resource: any) => resource.firstName,
        resourceAccessor: (event: any) => event.staffMemberId,
      }
    : {},
  [isByStaff, visibleStaffForResources],
);
```

**Edit 5 — Extract handlers + memoize calendarComponents.** This is the load-bearing edit (RBC issue #2588). Two related changes at the JSX call site near line 1098–1136:

5a. Above the `return (...)` (insert just before the existing `filterPopover` definition at line 947, or right after the resourceProps useMemo from Edit 4 — pick the location that keeps related hooks together):

```typescript
const handleViewChange = useCallback((v: string) => {
  setCurrentView(v);
  if (v !== 'day') setIsByStaff(false);
}, []);

const handleByStaff = useCallback((active: boolean) => {
  setIsByStaff(active);
  if (active) setCurrentView(Views.DAY);
}, []);
```

5b. Below the `filterPopover` JSX block (around line 1049, before `return (`), add:

```typescript
const calendarComponents = useMemo(() => ({
  event: EventComponent as any,
  toolbar: ((toolbarProps: any) => (
    <CalendarToolbar
      {...toolbarProps}
      isByStaff={isByStaff}
      onByStaff={handleByStaff}
      filterControl={filterPopover}
    />
  )) as any,
}), [isByStaff, handleByStaff, filterPopover]);
```

5c. In the JSX, replace the inline `onView={(v: string) => { setCurrentView(v); if (v !== 'day') setIsByStaff(false); }}` (lines 1110–1113) with:
```typescript
onView={handleViewChange as any}
```

5d. In the JSX, replace the inline `components={{ event: EventComponent as any, toolbar: ((toolbarProps: any) => (...)) as any }}` block (lines 1117–1130) with:
```typescript
components={calendarComponents}
```

**Important caveats:**
- Per CONTEXT.md D-02, do NOT touch unrelated code. Do NOT re-style the modal, toolbar, filter popover. Do NOT extract components to separate files. Do NOT add `key={view-isByStaff}` (that is Wave 3 conditional escalation).
- Per RESEARCH.md, `filterPopover` is itself JSX rebuilt every render — accepting this for now. If diagnosis later shows EventComponent still re-mounts, a follow-up would memoize filterPopover. Out of scope for this task.
- Keep all existing `as any` casts where they appear; do not refactor types in this phase.
- Run `npm run check` after the edits. Fix any TypeScript errors before committing. Common pitfall: forgetting to add `useCallback` to imports — TS will surface this immediately.
- Run `npm run build` to verify no broken imports.
- Commit with message: `fix(phase-20): memoize calendar components, resourceProps; remove redundant view reset (RBC #2588)` and reference RESEARCH.md hot spots #1, #2, #6.

This task implements RESEARCH.md Suspect Hot Spots #1 (delete useEffect), #2 (memoize resourceProps), and #6 (memoize components), plus the useCallback recommendation from RESEARCH.md § Open Questions item 3.
  </action>
  <verify>
    <automated>npm run check &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0 (TypeScript compiles cleanly)
    - `npm run build` exits 0 (production build succeeds — verifies no broken imports)
    - `grep -c "useMemo" client/src/components/admin/AppointmentsCalendarSection.tsx` returns at least `3` (visibleStaffForResources, resourceProps, calendarComponents)
    - `grep -c "useCallback" client/src/components/admin/AppointmentsCalendarSection.tsx` returns at least `2` (handleViewChange, handleByStaff)
    - `grep "setCurrentView(DEFAULT_CALENDAR_VIEW)" client/src/components/admin/AppointmentsCalendarSection.tsx` returns 0 hits inside a useEffect — i.e., `grep -B1 -A1 "setCurrentView(DEFAULT_CALENDAR_VIEW)" client/src/components/admin/AppointmentsCalendarSection.tsx | grep -c "useEffect"` returns 0
    - `grep "components={calendarComponents}" client/src/components/admin/AppointmentsCalendarSection.tsx` returns 1 hit (the JSX call site uses the memoized object)
    - `grep "onView={handleViewChange" client/src/components/admin/AppointmentsCalendarSection.tsx` returns 1 hit
    - `grep -E "const resourceProps = useMemo" client/src/components/admin/AppointmentsCalendarSection.tsx` returns 1 hit
    - `grep -E "const visibleStaffForResources = useMemo" client/src/components/admin/AppointmentsCalendarSection.tsx` returns 1 hit
  </acceptance_criteria>
  <done>
All 5 edits applied; type check + build both pass. The component now passes stable references for `components`, `resourceProps`, `onView`, and `visibleStaffForResources`. The redundant on-mount useEffect is gone. Ready for re-measurement.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-run diagnostic measurement after Wave 1 fixes; record in 20-DIAGNOSIS.md § Post-Wave-1</name>
  <files>.planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md</files>
  <read_first>
    - .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md (§ Baseline — to compute Δ)
    - .planning/phases/20-calendar-timeline-structure-audit/20-RESEARCH.md (§ Diagnostic Snippet, lines 318–342)
    - .planning/phases/20-calendar-timeline-structure-audit/20-CONTEXT.md (D-05 zoom matrix)
  </read_first>
  <action>
Manual browser verification task — see <how-to-verify> below for the exact procedure. Claude cannot automate browser DevTools, browser zoom, or visual inspection; the human executor performs the steps and records results in the file listed in <files>. The orchestrator must surface the <how-to-verify> instructions to the human and wait for the resume-signal.
  </action>
  <what-built>
The AppointmentsCalendarSection.tsx file now has: memoized components, memoized resourceProps, memoized visibleStaffForResources, useCallback handlers, and no on-mount setCurrentView useEffect. Type check + build pass. Calendar should render visually identical to before (no behavior change), but RBC's internal `mergeComponents()` no longer re-runs on every parent render.
  </what-built>
  <how-to-verify>
This is a **manual browser task** — the human re-runs the same 9-cell measurement matrix from Wave 0. Steps:

1. Run `npm run dev` (port 5000) — restart if it was running before the edits, to ensure the rebuild is fresh.
2. Open admin calendar (http://localhost:5000/admin → Calendar / Planning Desk).
3. Open DevTools Console.
4. For each of the 9 (zoom × view) cells (same matrix as Wave 0):
   - Set zoom (75%, 100%, 125%).
   - Switch view (Day, Week, or Day + ByStaff).
   - Wait for calendar to settle.
   - Paste the snippet from `20-DIAGNOSIS.md § DevTools Snippet` (or RESEARCH.md lines 322–339).
   - Read `MAX offset` and `MEAN offset` from console.
   - Open `20-DIAGNOSIS.md` and fill the corresponding row of the **## Post-Wave-1** table:
     - `MAX offset` column: e.g., `0.05px`
     - `MEAN offset` column: e.g., `0.01px`
     - `Δ vs Baseline` column: signed delta (e.g., `-0.45px` if MAX dropped from 0.50 to 0.05)
     - `Pass/Fail` column: `PASS` if `|MAX| ≤ 1.0`, else `FAIL`

5. After all 9 cells filled, write **### Post-Wave-1 observations** (1–3 sentences):
   - Did Wave 1 fully resolve alignment? (Δ direction summary, all-cells pass / partial / no-change)
   - Any residual offsets? Where? (zoom × view combos)
   - Visible UX changes? (e.g., calendar no longer flickers when toggling filter pills — confirms Suspect #2/#6 was real)

6. Commit with message: `docs(phase-20): record Post-Wave-1 diagnosis after memoization fixes`.

**Decision routing for Plan 03 (informational — not done in this task):**
- If all 9 Post-Wave-1 cells PASS → Plan 03 Task 1 selects Strategy A (no CSS change).
- If pattern matches "uniform at 100%, drift at 75/125%" → Plan 03 Task 1 selects Strategy D (Pitfall 5).
- If pattern matches "fail only in By Staff view" → Plan 03 Task 1 selects Strategy C (sticky-positioning interaction).
- Otherwise → Plan 03 Task 1 selects Strategy B (flex anchor + negative margin).
The decision rule is in 20-DIAGNOSIS.md § Strategy Decision and copied verbatim from RESEARCH.md lines 484–488.

**Expected outcome:** All 9 rows of the **## Post-Wave-1** table contain MAX/MEAN/Δ/Pass-Fail; **### Post-Wave-1 observations** has a written summary.
  </how-to-verify>
  <verify>
    <automated>grep -A 20 "## Post-Wave-1" .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md | grep -c "PASS\|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Inside the `## Post-Wave-1` section of 20-DIAGNOSIS.md, at least `9` PASS/FAIL markers exist (one per row)
    - At least 9 numeric `Δ vs Baseline` entries (regex: `[-+]?[0-9]+\.[0-9]+\s*px` appears at least 27 times across MAX + MEAN + Δ columns of the Post-Wave-1 table)
    - `### Post-Wave-1 observations` section contains at least one non-template sentence
    - `npm run check` still exits 0 (no regression introduced by re-measurement work — this is a docs-only change but verify nothing else slipped)
  </acceptance_criteria>
  <resume-signal>Type "post-wave-1-recorded" when all 9 rows of Post-Wave-1 are filled and observations are written. Optionally include the headline finding (e.g., "all pass — Strategy A" or "still failing in By Staff at 75% — likely Strategy D").</resume-signal>
  <done>
20-DIAGNOSIS.md § Post-Wave-1 contains 9 measurement rows + observations. Plan 03 (Wave 2) now has the data needed to pick a CSS strategy.
  </done>
</task>

</tasks>

<verification>
- Run `npm run check` — must pass
- Run `npm run build` — must pass
- Run `grep -c "useMemo\|useCallback" client/src/components/admin/AppointmentsCalendarSection.tsx` — should be at least 5
- Run `grep -A 20 "## Post-Wave-1" .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md | grep -c "PASS\|FAIL"` — must return ≥ 9
</verification>

<success_criteria>
- 5 structural fixes applied to AppointmentsCalendarSection.tsx
- Type check and production build both pass
- Post-Wave-1 measurements recorded in 20-DIAGNOSIS.md
- Δ vs Baseline computed for each of 9 cells
- Observations written attributing changes (or non-changes) to the fixes
</success_criteria>

<output>
After completion, create `.planning/phases/20-calendar-timeline-structure-audit/20-02-SUMMARY.md` documenting:
- 5 edits applied (with line ranges)
- Post-Wave-1 measurement summary (e.g., "all cells now passing — Strategy A recommended for Plan 03")
- Headline recommendation for Plan 03 Task 1 (which strategy the data supports)
- Any unexpected findings (e.g., "filter-toggle flicker eliminated — confirms Suspect #6")
</output>
