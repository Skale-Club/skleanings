---
phase: 20-calendar-timeline-structure-audit
plan: 04
type: execute
wave: 3
depends_on: [20-03]
files_modified:
  - client/src/components/admin/AppointmentsCalendarSection.tsx
  - .planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md
  - .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md
autonomous: false
requirements: [CAL-FIX-01, CAL-FIX-02, CAL-FIX-03, CAL-FIX-04]
must_haves:
  truths:
    - "Conditional `key={`${currentView}-${isByStaff}`}` remount is applied IF AND ONLY IF Plan 03 Task 3 observations indicated escalation needed (or D-06 view-switch matrix fails). If skipped, the skip is documented."
    - "20-HUMAN-UAT.md § CAL-FIX-01 marked PASS by human (with concrete result data — final Post-Wave-2 numbers reproduced or referenced)"
    - "20-HUMAN-UAT.md § CAL-FIX-02 marked PASS by human (D-06 view-switch sequence walked successfully)"
    - "20-HUMAN-UAT.md § CAL-FIX-03 marked PASS by human (5+ staff By Staff view + horizontal scroll + filter toggle + sticky gutter all confirmed)"
    - "20-HUMAN-UAT.md § CAL-FIX-04 marked PASS by human with all 5 Phase 19 sub-items individually marked PASS"
    - "Type check + build still pass after the optional remount edit"
  artifacts:
    - path: "client/src/components/admin/AppointmentsCalendarSection.tsx"
      provides: "Conditional `key` prop on <DnDCalendar> if escalation triggered"
      contains_one_of:
        - "key={`${currentView}-${isByStaff}`}"
        - "// Phase 20: key remount NOT applied — diagnosis showed all cells passing"
    - path: ".planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md"
      provides: "All 4 UAT entries marked PASS with result data"
      contains: "result: PASS"
  key_links:
    - from: "20-DIAGNOSIS.md § Post-Wave-2 observations"
      to: "Plan 04 Task 1 conditional decision (apply remount or skip)"
      via: "Observations dictate whether Task 1 is a code edit or a documented skip"
      pattern: "Wave 3"
    - from: "20-HUMAN-UAT.md § CAL-FIX-04"
      to: "19-HUMAN-UAT.md (5 items)"
      via: "Plan 04 Task 5 walks each of the 5 items and records per-item PASS"
      pattern: "19-UAT-"
---

<objective>
Wave 3 — View-switch hygiene (conditional) + full UAT signoff. First, decide whether to apply the conditional `key={`${currentView}-${isByStaff}`}` remount based on Plan 03's Post-Wave-2 observations. Then run the full UAT matrix in `20-HUMAN-UAT.md`: CAL-FIX-01 (zoom matrix), CAL-FIX-02 (D-06 view-switch sequence), CAL-FIX-03 (By Staff multi-column), and CAL-FIX-04 (Phase 19 regression — all 5 items individually attributed).

Purpose: This is the phase gate. If all 4 UAT entries pass, the phase is shippable. If any fails, the failure is captured for `/gsd:diagnose` to plan a gap-closure phase.

Output:
- (Conditional) `client/src/components/admin/AppointmentsCalendarSection.tsx` with `key={`${currentView}-${isByStaff}`}` on `<DnDCalendar>` — OR — a comment line documenting the deliberate skip
- `.planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md` with all 4 entries filled with PASS/FAIL + result data
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-calendar-timeline-structure-audit/20-CONTEXT.md
@.planning/phases/20-calendar-timeline-structure-audit/20-RESEARCH.md
@.planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md
@.planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md
@.planning/phases/20-calendar-timeline-structure-audit/20-03-SUMMARY.md
@.planning/phases/19-receptionist-booking-flow-multi-staff-view/19-HUMAN-UAT.md
@client/src/components/admin/AppointmentsCalendarSection.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CONDITIONAL — Apply key={`${currentView}-${isByStaff}`} on <DnDCalendar> if Plan 03 observations escalated; otherwise document skip</name>
  <files>client/src/components/admin/AppointmentsCalendarSection.tsx</files>
  <read_first>
    - .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md (§ Post-Wave-2 observations — the routing decision lives here)
    - .planning/phases/20-calendar-timeline-structure-audit/20-RESEARCH.md (§ Architecture Patterns — Pattern 4: Force remount on view+resources change, lines 236–249; including the tradeoff note about scroll position destruction)
    - .planning/phases/20-calendar-timeline-structure-audit/20-CONTEXT.md (Claude's Discretion: "key={view-isByStaff}" — escalation only, measure first)
    - client/src/components/admin/AppointmentsCalendarSection.tsx (specifically lines 1098–1136, the `<DnDCalendar>` JSX)
  </read_first>
  <action>
Read `20-DIAGNOSIS.md § Post-Wave-2 observations` to determine the routing.

**If the observations say "all cells passing — Wave 3 conditional `key={view-isByStaff}` remount NOT needed" (or equivalent):**

Add a single-line comment to `client/src/components/admin/AppointmentsCalendarSection.tsx` immediately above the `<DnDCalendar>` opening tag (currently line 1098). The comment must be exactly:
```typescript
{/* Phase 20: key remount NOT applied — Wave 1 + Wave 2 diagnosis showed all cells passing. See 20-DIAGNOSIS.md § Post-Wave-2. */}
```
This documents the deliberate skip so a future reader doesn't reintroduce the remount unnecessarily.

Then run `npm run check` and `npm run build` to confirm. Skip the rest of this task.

--- 

**If the observations say "residual offsets > 1px" OR "Wave 3 should activate" OR D-06 view-switch matrix shows visible stale layout:**

Add a `key` prop to `<DnDCalendar>`. The exact change at line 1098:

BEFORE:
```typescript
<DnDCalendar
  {...resourceProps}
  draggableAccessor={...}
```

AFTER:
```typescript
<DnDCalendar
  key={`${currentView}-${isByStaff}`}
  {...resourceProps}
  draggableAccessor={...}
```

The `key` MUST be the FIRST prop (before `{...resourceProps}` spread) so React reads it before any other prop deserialization.

**Tradeoff acknowledgment:** Per RESEARCH.md Pattern 4, this remount destroys RBC's scroll position on view change. This is acceptable for this phase because:
- View changes are user-initiated, not automatic
- `scrollToTime={DEFAULT_SCROLL_TIME}` already re-applies the scroll target on remount
- Issue #2260 already affects scroll position on DnD interaction, so users are not relying on persistent scroll across view switches

If during testing the scroll-jump UX feels excessive, a follow-up phase can investigate finer-grained invalidation. For now, the remount is the documented escalation.

Run `npm run check` and `npm run build`. Commit with message: `fix(phase-20): force <DnDCalendar> remount on view+resource change for stale layout (Wave 3 escalation)`.
  </action>
  <verify>
    <automated>npm run check &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0
    - `npm run build` exits 0
    - EXACTLY ONE of the following is true (verifiable by grep):
      - `grep "key={\`\${currentView}-\${isByStaff}\`}" client/src/components/admin/AppointmentsCalendarSection.tsx` returns 1 hit (escalation applied)
      - `grep "Phase 20: key remount NOT applied" client/src/components/admin/AppointmentsCalendarSection.tsx` returns 1 hit (skip documented)
    - If escalation applied: `grep -A 1 "<DnDCalendar" client/src/components/admin/AppointmentsCalendarSection.tsx | grep "key=" | head -1` shows the key prop appears immediately after `<DnDCalendar`
  </acceptance_criteria>
  <done>
Either the conditional `key` prop is applied on `<DnDCalendar>` (with type check + build passing), or a deliberate-skip comment is in place. The decision is traceable to 20-DIAGNOSIS.md § Post-Wave-2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run CAL-FIX-01 zoom × view UAT matrix in browser; record PASS/FAIL in 20-HUMAN-UAT.md</name>
  <files>.planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md (§ CAL-FIX-01)
    - .planning/phases/20-calendar-timeline-structure-audit/20-VALIDATION.md (§ Manual-Only Verifications, CAL-FIX-01 row)
    - .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md (§ Post-Wave-2 — final measurement evidence)
  </read_first>
  <action>
Manual browser verification task — see <how-to-verify> below for the exact procedure. Claude cannot automate browser DevTools, browser zoom, or visual inspection; the human executor performs the steps and records results in the file listed in <files>. The orchestrator must surface the <how-to-verify> instructions to the human and wait for the resume-signal.
  </action>
  <what-built>
Calendar with all Wave 1 + Wave 2 + (conditional) Wave 3 fixes applied. Type check + build pass.
  </what-built>
  <how-to-verify>
**Manual browser task.** This task is the formal sign-off for CAL-FIX-01. Steps:

1. Run `npm run dev` and open admin calendar.
2. Open DevTools Console.
3. For each of the 9 (zoom × view) cells of the matrix (75/100/125% × Day/Week/Day+ByStaff):
   - Set zoom; switch view; paste the diagnostic snippet (from `20-DIAGNOSIS.md § DevTools Snippet`).
   - Confirm `MAX |offset| ≤ 1.0 px`.
   - Visually confirm time labels (e.g., "9:00 AM") appear horizontally aligned with the corresponding grid line — no obvious drift.
4. Open `20-HUMAN-UAT.md` and update **### CAL-FIX-01**:
   - Replace `result: [pending]` with EITHER:
     - `result: PASS — all 9 cells MAX |offset| ≤ 1px (see 20-DIAGNOSIS.md § Post-Wave-2)` (if all pass)
     - `result: FAIL — cells [list]` followed by the specific failures + offset values (if any fail)
5. Update the frontmatter `updated:` field to current ISO timestamp.
6. Update **## Summary** counters: increment `passed:` or `issues:` accordingly.
7. Commit: `test(phase-20): record CAL-FIX-01 UAT result`.

**Pass criterion (verbatim from VALIDATION.md):** For each (view × zoom) cell, MAX |offset| ≤ 1 px subpixel rounding tolerance. > 1px on any cell is FAIL.

If FAIL: do NOT proceed to subsequent tasks — gap-closure planning needed. Surface the failure to the user.
  </how-to-verify>
  <verify>
    <automated>grep -A 2 "^### CAL-FIX-01" .planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md | grep -c "result: PASS\|result: FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 2 "^### CAL-FIX-01" 20-HUMAN-UAT.md | grep -c "result: PASS\|result: FAIL"` returns 1
    - Result line includes either "all 9 cells" or specific cell list (regex: matches one of `9 cells`, `75%`, `100%`, `125%` in the result line)
    - 20-HUMAN-UAT.md frontmatter `updated:` timestamp is today's date
  </acceptance_criteria>
  <resume-signal>Type "cal-fix-01-pass" or "cal-fix-01-fail" with details. If FAIL, also note which cells failed and by how much.</resume-signal>
  <done>
CAL-FIX-01 marked PASS or FAIL in 20-HUMAN-UAT.md with concrete evidence. Phase status reflects this.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run CAL-FIX-02 view-switch matrix (D-06 sequence) in browser; record PASS/FAIL in 20-HUMAN-UAT.md</name>
  <files>.planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md (§ CAL-FIX-02)
    - .planning/phases/20-calendar-timeline-structure-audit/20-VALIDATION.md (§ Manual-Only Verifications, CAL-FIX-02 row)
    - .planning/phases/20-calendar-timeline-structure-audit/20-CONTEXT.md (D-06 sequence)
  </read_first>
  <action>
Manual browser verification task — see <how-to-verify> below for the exact procedure. Claude cannot automate browser DevTools, browser zoom, or visual inspection; the human executor performs the steps and records results in the file listed in <files>. The orchestrator must surface the <how-to-verify> instructions to the human and wait for the resume-signal.
  </action>
  <what-built>
Calendar with stable component identity (Wave 1) + CSS strategy (Wave 2) + (conditional) view-resource remount (Wave 3 if escalated). View-switch staleness should be eliminated.
  </what-built>
  <how-to-verify>
**Manual browser task.** Walk the D-06 view-switch sequence. Steps:

1. Run `npm run dev` and open admin calendar.
2. Set zoom to 100%, view to Month (the starting state of the D-06 sequence).
3. Walk the sequence: Month → Week → Day → By Staff (Day + isByStaff toggle on) → Week (Day + isByStaff toggle off implicitly via D-11 handler) → Month.
4. After EACH transition, perform 4 visual checks (verbatim from VALIDATION.md):
   - (a) `.rbc-time-gutter` width is consistent with the previous view (no jump).
   - (b) `.rbc-header` row positions match `.rbc-day-slot` columns (no offset).
   - (c) Event blocks sit within their slot boundaries (no events spilling into adjacent rows/columns).
   - (d) No manual page reload was needed (calendar self-heals on view change).
5. Open `20-HUMAN-UAT.md` and update **### CAL-FIX-02**:
   - `result: PASS — all 5 transitions clean across 4 visual checks` (if all pass)
   - `result: FAIL — transition [X→Y] failed check [a/b/c/d]: [description]` (if any fail)
6. Update frontmatter `updated:`. Increment Summary counters.
7. Commit: `test(phase-20): record CAL-FIX-02 UAT result`.

**Pass criterion (verbatim from VALIDATION.md):** All 4 visual checks pass at every one of the 5 transitions.

If FAIL: surface to user. If the failure mode matches "Wave 3 conditional remount needed but Plan 04 Task 1 skipped it", that's a planning bug — surface explicitly.
  </how-to-verify>
  <verify>
    <automated>grep -A 2 "^### CAL-FIX-02" .planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md | grep -c "result: PASS\|result: FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 2 "^### CAL-FIX-02" 20-HUMAN-UAT.md | grep -c "result: PASS\|result: FAIL"` returns 1
    - Result line mentions either "5 transitions" / "all transitions" (PASS) or names a specific transition (FAIL — regex matches one of `Month`, `Week`, `Day`, `By Staff` in the result line)
  </acceptance_criteria>
  <resume-signal>Type "cal-fix-02-pass" or "cal-fix-02-fail" with details.</resume-signal>
  <done>
CAL-FIX-02 marked PASS or FAIL with concrete evidence about the D-06 sequence.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Run CAL-FIX-03 By Staff multi-column UAT (5+ staff) in browser; record PASS/FAIL</name>
  <files>.planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md (§ CAL-FIX-03)
    - .planning/phases/20-calendar-timeline-structure-audit/20-VALIDATION.md (§ Manual-Only Verifications, CAL-FIX-03 row)
    - .planning/phases/19-receptionist-booking-flow-multi-staff-view/19-CONTEXT.md (D-02 resourceProps shape — context for what By Staff should look like)
  </read_first>
  <action>
Manual browser verification task — see <how-to-verify> below for the exact procedure. Claude cannot automate browser DevTools, browser zoom, or visual inspection; the human executor performs the steps and records results in the file listed in <files>. The orchestrator must surface the <how-to-verify> instructions to the human and wait for the resume-signal.
  </action>
  <what-built>
Memoized resourceProps + visibleStaffForResources + (conditional) remount on isByStaff change. Horizontal scroll inline `style={{ overflowX: isByStaff ? 'auto' : undefined }}` is unchanged (per CONTEXT.md narrow scope — RESEARCH.md noted moving to className was conditional and Plan 03 may not have triggered it).
  </what-built>
  <how-to-verify>
**Manual browser task.** Steps:

1. Ensure the test environment has 5+ staff members in the database. If fewer than 5 staff exist, seed via the admin Staff page or SQL until at least 5 are present (NOT a fix — just test data).
2. Run `npm run dev` and open admin calendar.
3. Set zoom to 100%, view to Day, toggle "By Staff" ON.
4. Perform 4 checks (verbatim from VALIDATION.md):
   - (a) Confirm one column per visible staff member (count columns; should equal `visibleStaffCount`).
   - (b) Confirm horizontal scrollbar appears on `.appointments-calendar-shell__board` (5 columns × 140px min-width = 700px exceeds typical viewport at 100% zoom).
   - (c) Toggle a staff filter (open Filters popover, click a staff FilterPill to hide them) — column should be removed without page reload. Toggle again to re-add — column reappears.
   - (d) During horizontal scroll, the time gutter remains sticky at left edge (its `position: sticky; left: 0` per RBC base CSS).
5. Update **### CAL-FIX-03** in 20-HUMAN-UAT.md:
   - `result: PASS — all 4 checks pass with N staff visible` (if all pass)
   - `result: FAIL — check [a/b/c/d]: [description]` (if any fail)
6. Update frontmatter `updated:`. Increment Summary counters.
7. Commit: `test(phase-20): record CAL-FIX-03 UAT result`.

**Pass criterion (verbatim from VALIDATION.md):** All 4 checks pass.
  </how-to-verify>
  <verify>
    <automated>grep -A 2 "^### CAL-FIX-03" .planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md | grep -c "result: PASS\|result: FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 2 "^### CAL-FIX-03" 20-HUMAN-UAT.md | grep -c "result: PASS\|result: FAIL"` returns 1
    - Result line mentions number of staff visible (regex: matches `[0-9]+ staff` or `5+ staff` or similar)
  </acceptance_criteria>
  <resume-signal>Type "cal-fix-03-pass" or "cal-fix-03-fail" with details.</resume-signal>
  <done>
CAL-FIX-03 marked PASS or FAIL with concrete evidence.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Run CAL-FIX-04 Phase 19 regression — re-walk all 5 items from 19-HUMAN-UAT.md; record PASS/FAIL per item in 20-HUMAN-UAT.md</name>
  <files>.planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md (§ CAL-FIX-04 with the 5-item attribution block)
    - .planning/phases/19-receptionist-booking-flow-multi-staff-view/19-HUMAN-UAT.md (the 5 items with their original `expected:` text)
    - .planning/phases/20-calendar-timeline-structure-audit/20-VALIDATION.md (§ Manual-Only Verifications, CAL-FIX-04 row)
    - .planning/phases/20-calendar-timeline-structure-audit/20-CONTEXT.md (D-07 regression check)
  </read_first>
  <action>
Manual browser verification task — see <how-to-verify> below for the exact procedure. Claude cannot automate browser DevTools, browser zoom, or visual inspection; the human executor performs the steps and records results in the file listed in <files>. The orchestrator must surface the <how-to-verify> instructions to the human and wait for the resume-signal.
  </action>
  <what-built>
The full set of Wave 1 + Wave 2 + (conditional) Wave 3 fixes is applied. Phase 19 functionality (drag-to-reassign, QuickBook, GCal busy guard, By Staff layout, customer staff badges) MUST still work — none of the fixes touched those code paths, but memoizing components could theoretically affect handler closures.
  </what-built>
  <how-to-verify>
**Manual browser task.** Walk each of the 5 Phase 19 UAT items exactly as written in `19-HUMAN-UAT.md`. Steps:

1. Open `19-HUMAN-UAT.md` to read the original 5 items' `expected:` text.
2. Run `npm run dev` and open admin calendar (and customer-side BookingPage for item 5).
3. For each item, perform the original Phase 19 verification:

   **19-UAT-1 (By Staff column layout):**
   - Open admin calendar in Day view; toggle "By Staff" ON.
   - Confirm RBC renders one column per visible staff; column headers show first names.
   - Mark PASS or FAIL.

   **19-UAT-2 (Quick Book 30-second walk-in flow):**
   - In By Staff view, click an empty slot in a staff column.
   - Confirm modal opens with staff and time pre-filled.
   - Submit; confirm POST /api/bookings fires (Network tab) and booking appears on calendar with `status=confirmed`.
   - Mark PASS or FAIL.

   **19-UAT-3 (Drag-to-reassign with undo toast):**
   - In By Staff view, drag an existing booking event to a different staff column.
   - Confirm PUT /api/bookings/:id fires with new `staffMemberId`.
   - Confirm undo toast appears; click undo within 5 seconds; confirm change is reversed.
   - Mark PASS or FAIL.

   **19-UAT-4 (GCal busy block not draggable):**
   - In any view with GCal busy events visible, attempt to drag a busy block.
   - Confirm drag does not start (event is non-draggable per `draggableAccessor={(e) => !e.isGcalBusy}`).
   - Confirm real bookings ARE draggable.
   - Mark PASS or FAIL.

   **19-UAT-5 (Customer step 3 staff badges):**
   - Navigate to customer-side `/booking` flow (BookingPage); progress to step 3 (time slot selection).
   - With a service that has multiple available staff, confirm time slots show staff name badges.
   - Verify no regression on a single-staff scenario (badge absent or shows the single staff name per Phase 19 spec).
   - Mark PASS or FAIL.

4. Open `20-HUMAN-UAT.md` and update **### CAL-FIX-04**:
   - For each of the 5 sub-items, replace `[pending]` with `PASS` or `FAIL — [description]`.
   - Set the top-level `result:` line to `PASS` IFF all 5 sub-items are PASS, otherwise `FAIL — see per-item breakdown`.
5. Update frontmatter `updated:`. Increment Summary counters (treat CAL-FIX-04 as a single PASS/FAIL for the entry, but the sub-item attribution is the audit trail).
6. Commit: `test(phase-20): record CAL-FIX-04 Phase 19 regression result`.

**Pass criterion (verbatim from VALIDATION.md):** All 5 Phase 19 items still pass identically to Phase 19 acceptance.

If any sub-item fails: regression introduced by Phase 20 fixes. Likely culprits (in order): handler closure changed by useCallback (check Task 1 of Plan 02), `key` prop forcing remount mid-drag (Plan 04 Task 1 escalation), CSS strategy affecting layout in unexpected way (Plan 03 Task 2). Surface the failure with attribution to the user.
  </how-to-verify>
  <verify>
    <automated>grep -A 10 "^### CAL-FIX-04" .planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md | grep -cE "PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Inside the `### CAL-FIX-04` block, at least `6` PASS/FAIL markers (5 sub-items + 1 top-level result)
    - Each of `19-UAT-1`, `19-UAT-2`, `19-UAT-3`, `19-UAT-4`, `19-UAT-5` lines has either PASS or FAIL (regex test against each)
    - Top-level `result:` line is consistent with sub-items (PASS only if all 5 are PASS)
    - 20-HUMAN-UAT.md ## Summary `passed:` count = 4 if all 4 entries PASS (i.e., CAL-FIX-01..04 all PASS); otherwise reflects actual pass count
  </acceptance_criteria>
  <resume-signal>Type "cal-fix-04-pass" if all 5 Phase 19 items still pass. If any fail, list which items failed and the apparent regression cause.</resume-signal>
  <done>
CAL-FIX-04 has all 5 sub-items individually marked PASS/FAIL with the top-level result reflecting the conjunction. The phase has full UAT coverage of all 4 requirements.
  </done>
</task>

</tasks>

<verification>
- Run `npm run check && npm run build` — must pass
- Run `grep -c "result: PASS\|result: FAIL" .planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md` — must return at least 4 (one per CAL-FIX-NN top-level)
- Run `grep -E "(key=\\\`\\\$\{currentView\}|key remount NOT applied)" client/src/components/admin/AppointmentsCalendarSection.tsx` — must return at least 1 hit (decision documented either way)
- Phase ships if all 4 entries are PASS; otherwise gap-closure planning is needed
</verification>

<success_criteria>
- Conditional `key` remount applied OR documented skip
- All 4 UAT entries marked PASS or FAIL with evidence
- CAL-FIX-04's 5 Phase 19 sub-items individually attributed
- Type check + build pass
- Summary counters updated to reflect outcomes
</success_criteria>

<output>
After completion, create `.planning/phases/20-calendar-timeline-structure-audit/20-04-SUMMARY.md` documenting:
- Wave 3 conditional decision (apply or skip remount; reason)
- UAT outcomes (CAL-FIX-01..04 PASS/FAIL)
- Per-Phase-19 sub-item outcomes (5 items × PASS/FAIL)
- Phase shippability assessment
- If any FAIL: clear attribution + recommendation for `/gsd:diagnose --gaps` follow-up
- Structural debt observed during audit (deferred — for future "Calendar Decomposition" phase per CONTEXT.md D-02)
</output>
