---
phase: 20-calendar-timeline-structure-audit
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md
  - .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md
autonomous: false
requirements: [CAL-FIX-01, CAL-FIX-02, CAL-FIX-03, CAL-FIX-04]
must_haves:
  truths:
    - "20-HUMAN-UAT.md exists with one entry per CAL-FIX-NN (4 total) and the exact pass criteria from VALIDATION.md § Manual-Only Verifications copied verbatim"
    - "20-DIAGNOSIS.md exists with a 9-cell baseline measurement table (3 zoom levels × 3 view configurations) recorded by the human running the DevTools snippet"
    - "Wave 1 fixes can be measured against the Wave 0 baseline (i.e., the baseline numbers are persisted before any code change)"
  artifacts:
    - path: ".planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md"
      provides: "4 UAT entries (CAL-FIX-01 through CAL-FIX-04) with explicit pass criteria"
      contains: "## CAL-FIX-01"
    - path: ".planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md"
      provides: "Baseline + post-Wave-1 + post-Wave-2 measurement tables; CSS strategy decision section"
      contains: "## Baseline"
  key_links:
    - from: "20-DIAGNOSIS.md § Baseline"
      to: "20-DIAGNOSIS.md § Strategy Decision (Plan 03)"
      via: "Wave 2 reads baseline + post-Wave-1 numbers to choose Strategy A/B/C/D"
      pattern: "Strategy Decision"
    - from: "20-HUMAN-UAT.md § CAL-FIX-04"
      to: "19-HUMAN-UAT.md (5 items)"
      via: "Plan 04 re-runs Phase 19 UAT and records per-item attribution under CAL-FIX-04"
      pattern: "Phase 19 regression"
---

<objective>
Wave 0 — Investigation-first setup. Create the two artifacts that gate the rest of the phase: `20-HUMAN-UAT.md` (the manual verification contract) and `20-DIAGNOSIS.md` (the persistent measurement record). Then perform the diagnostic browser-DevTools measurement pass at the full zoom × view matrix and persist the baseline numbers in `20-DIAGNOSIS.md`.

Purpose: Per CONTEXT.md D-01, the plan opens with diagnosis, NOT fixes. Without a baseline measurement, all CSS choices in Wave 2 are guesses (RESEARCH.md § Open Questions, item 1). This plan creates the evidence floor that Wave 1's fixes will be measured against.

Output:
- `.planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md` (4 UAT entries with verbatim pass criteria from VALIDATION.md)
- `.planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md` (skeleton + Wave 0 baseline measurements at 75/100/125% × Day/Week/Day+ByStaff)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-calendar-timeline-structure-audit/20-CONTEXT.md
@.planning/phases/20-calendar-timeline-structure-audit/20-RESEARCH.md
@.planning/phases/20-calendar-timeline-structure-audit/20-VALIDATION.md
@.planning/phases/19-receptionist-booking-flow-multi-staff-view/19-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 20-HUMAN-UAT.md with 4 UAT entries (CAL-FIX-01 through CAL-FIX-04)</name>
  <files>.planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/20-calendar-timeline-structure-audit/20-VALIDATION.md (§ Manual-Only Verifications — copy pass criteria VERBATIM)
    - .planning/phases/20-calendar-timeline-structure-audit/20-CONTEXT.md (D-04, D-05, D-06, D-07)
    - .planning/phases/19-receptionist-booking-flow-multi-staff-view/19-HUMAN-UAT.md (format mirror — same frontmatter shape, same `### N. Title` / `expected:` / `result:` structure)
  </read_first>
  <action>
Create `.planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md` mirroring Phase 19's UAT format. Use this exact structure:

```markdown
--- 
status: pending
phase: 20-calendar-timeline-structure-audit
source: [20-VALIDATION.md, 20-CONTEXT.md]
started: 2026-05-05T00:00:00.000Z
updated: 2026-05-05T00:00:00.000Z
--- 

## Current Test

[awaiting human testing]

## Tests

### CAL-FIX-01. Time gutter labels align with grid line at all zoom levels
expected: For each (view × zoom) cell of the matrix Day/Week/Day+ByStaff at 75/100/125%, run the DevTools `getBoundingClientRect` snippet from `20-RESEARCH.md` § Diagnostic Snippet on `.rbc-label` and the corresponding `.rbc-timeslot-group` top; record offset; pass = 0 px ± 1 px subpixel rounding; fail = > 1 px on any cell
result: [pending]

### CAL-FIX-02. View-switch leaves no stale layout
expected: Walk through Month → Week → Day → By Staff → Week → Month in one continuous browser session; after each transition: (a) inspect `.rbc-time-gutter` width is consistent, (b) inspect `.rbc-header` row positions match `.rbc-day-slot` columns, (c) verify event blocks sit within their slot boundaries, (d) confirm no manual page reload was needed; pass = all 4 visual checks pass at every transition
result: [pending]

### CAL-FIX-03. By Staff multi-column view + horizontal scroll
expected: With 5+ staff visible, enter By Staff view: (a) confirm one column per visible staff, (b) confirm horizontal scrollbar appears on `.appointments-calendar-shell__board`, (c) toggle a staff filter — column should be added/removed without page reload, (d) confirm gutter remains sticky at left during horizontal scroll; pass = all 4
result: [pending]

### CAL-FIX-04. Phase 19 UAT regression check
expected: Walk through each of the 5 items in `.planning/phases/19-receptionist-booking-flow-multi-staff-view/19-HUMAN-UAT.md` exactly as written; pass = all 5 still pass identically to Phase 19 acceptance. Per-item attribution required:
  - 19-UAT-1 (By Staff column layout): [pending]
  - 19-UAT-2 (Quick Book 30-second walk-in flow): [pending]
  - 19-UAT-3 (Drag-to-reassign with undo toast): [pending]
  - 19-UAT-4 (GCal busy block not draggable): [pending]
  - 19-UAT-5 (Customer step 3 staff badges): [pending]
result: [pending]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps
```

Pass criteria text MUST be copy-pasted verbatim from `20-VALIDATION.md` § Manual-Only Verifications (the table's "Test Instructions" column for each requirement). Do not summarize or paraphrase. The `### CAL-FIX-04` per-item attribution structure is novel here — needed because Phase 19 UAT has 5 separate items that must each be re-checked.
  </action>
  <verify>
    <automated>test -f .planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md &amp;&amp; grep -c "^### CAL-FIX-" .planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md` exists
    - `grep -c "^### CAL-FIX-" 20-HUMAN-UAT.md` returns `4` (one entry per requirement)
    - `grep "## CAL-FIX-04" 20-HUMAN-UAT.md` finds the per-item attribution block (or `grep "19-UAT-1"` returns 1)
    - `grep "75/100/125" 20-HUMAN-UAT.md` returns at least 1 hit (zoom matrix from D-05 captured under CAL-FIX-01)
    - `grep "Month → Week → Day → By Staff" 20-HUMAN-UAT.md` returns 1 hit (D-06 sequence under CAL-FIX-02)
    - `grep "phase: 20-calendar-timeline-structure-audit" 20-HUMAN-UAT.md` returns 1 (frontmatter present)
  </acceptance_criteria>
  <done>
20-HUMAN-UAT.md exists with 4 UAT entries (CAL-FIX-01 through CAL-FIX-04), pass criteria copied verbatim from VALIDATION.md, frontmatter mirroring Phase 19 format, and the CAL-FIX-04 per-item attribution block ready for Plan 04 to fill in.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 20-DIAGNOSIS.md skeleton with measurement tables and strategy decision section</name>
  <files>.planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md</files>
  <read_first>
    - .planning/phases/20-calendar-timeline-structure-audit/20-VALIDATION.md (§ Wave 0 Requirements — DIAGNOSIS.md is required artifact)
    - .planning/phases/20-calendar-timeline-structure-audit/20-RESEARCH.md (§ Diagnostic Snippet, lines 318–342; § Pixel-Alignment Strategies for `.rbc-label`, lines 418–488; § Suspect Hot Spots, lines 600–616)
    - .planning/phases/20-calendar-timeline-structure-audit/20-CONTEXT.md (D-01 investigation-first; D-03 root-cause over surface-fix)
  </read_first>
  <action>
Create `.planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md` as the persistent diagnosis record. Use this exact structure:

```markdown
--- 
phase: 20-calendar-timeline-structure-audit
status: skeleton
created: 2026-05-05
last_updated: 2026-05-05
--- 

# Phase 20 — Diagnosis

> Persistent record of pre-fix measurements, post-Wave-1 measurements, post-Wave-2 measurements, and the CSS strategy decision. All numbers come from running the DevTools `getBoundingClientRect` snippet documented in `20-RESEARCH.md` § Diagnostic Snippet (lines 318–342) on the live admin calendar.

## DevTools Snippet (paste into browser console)

```javascript
// Run in DevTools Console while admin calendar is open
const labels = document.querySelectorAll('.appointments-calendar .rbc-time-gutter .rbc-label');
const groups = document.querySelectorAll('.appointments-calendar .rbc-time-gutter .rbc-timeslot-group');
const offsets = [];
[...labels].forEach((label, i) => {
  const group = groups[i];
  if (!group) return;
  const labelRect = label.getBoundingClientRect();
  const groupRect = group.getBoundingClientRect();
  const labelCenter = labelRect.top + labelRect.height / 2;
  const groupTop = groupRect.top;
  const offset = labelCenter - groupTop;
  offsets.push(offset);
  console.log(
    `Label ${i} (${label.textContent}): label-center=${labelCenter.toFixed(2)} group-top=${groupTop.toFixed(2)} offset=${offset.toFixed(2)}px`
  );
});
console.log(`MAX offset: ${Math.max(...offsets.map(Math.abs)).toFixed(2)}px`);
console.log(`MEAN offset: ${(offsets.reduce((a, b) => a + b, 0) / offsets.length).toFixed(2)}px`);
```

Pass criterion (per CAL-FIX-01 / VALIDATION.md): MAX |offset| ≤ 1 px.

## Baseline (Wave 0 — pre-fix)

| Zoom | View          | MAX offset | MEAN offset | Pass/Fail | Notes |
|------|---------------|------------|-------------|-----------|-------|
| 75%  | Day           |            |             |           |       |
| 75%  | Week          |            |             |           |       |
| 75%  | Day + ByStaff |            |             |           |       |
| 100% | Day           |            |             |           |       |
| 100% | Week          |            |             |           |       |
| 100% | Day + ByStaff |            |             |           |       |
| 125% | Day           |            |             |           |       |
| 125% | Week          |            |             |           |       |
| 125% | Day + ByStaff |            |             |           |       |

### Baseline observations

- (To fill in by Wave 0 measurer — note any visible regressions, suspect hot spots from RESEARCH.md § Suspect Hot Spots that match the symptom, screenshots if helpful)

## Post-Wave-1 (after memoize + delete redundant useEffect)

| Zoom | View          | MAX offset | MEAN offset | Δ vs Baseline | Pass/Fail |
|------|---------------|------------|-------------|---------------|-----------|
| 75%  | Day           |            |             |               |           |
| 75%  | Week          |            |             |               |           |
| 75%  | Day + ByStaff |            |             |               |           |
| 100% | Day           |            |             |               |           |
| 100% | Week          |            |             |               |           |
| 100% | Day + ByStaff |            |             |               |           |
| 125% | Day           |            |             |               |           |
| 125% | Week          |            |             |               |           |
| 125% | Day + ByStaff |            |             |               |           |

### Post-Wave-1 observations

- (Did memoization alone resolve the offset? Δ direction? Any new artifacts?)

## Strategy Decision (Plan 03 — gate for Wave 2 CSS work)

Decision rule (copy from RESEARCH.md § Pixel-Alignment Strategies, lines 484–488):
- **Strategy A (keep Phase 18 transform):** Diagnosis shows uniform offset within ±1px at all zoom levels after Wave 1. No further CSS change.
- **Strategy B (flex anchor + negative margin):** Diagnosis shows transform-related drift inside sticky parent (drift only in By Staff at non-100% zoom).
- **Strategy C (absolute position inside first slot):** Diagnosis shows offset only in By Staff view; sticky-positioning + transform interaction suspected.
- **Strategy D (match gutter slot heights):** Diagnosis shows uniform offset at 100% but drift at 75/125% — indicates Pitfall 5 height mismatch.

**Selected strategy:** _____________________ (filled by Plan 03 Task 1)

**Justification:** _____________________ (cite which post-Wave-1 measurement supports the choice)

## Post-Wave-2 (after CSS strategy applied)

| Zoom | View          | MAX offset | MEAN offset | Δ vs Post-Wave-1 | Pass/Fail |
|------|---------------|------------|-------------|------------------|-----------|
| 75%  | Day           |            |             |                  |           |
| 75%  | Week          |            |             |                  |           |
| 75%  | Day + ByStaff |            |             |                  |           |
| 100% | Day           |            |             |                  |           |
| 100% | Week          |            |             |                  |           |
| 100% | Day + ByStaff |            |             |                  |           |
| 125% | Day           |            |             |                  |           |
| 125% | Week          |            |             |                  |           |
| 125% | Day + ByStaff |            |             |                  |           |

### Post-Wave-2 observations

- (If still > 1 px on any cell, escalate to Wave 3 conditional `key={view-isByStaff}` remount task)

## Structural Debt Documented (deferred — informational)

Per CONTEXT.md D-02, document any structural debt observed during diagnosis that does NOT block CAL-FIX-01 to -04 (so a future "Calendar Decomposition" phase has a starting point). Examples:

- 1556-line `AppointmentsCalendarSection.tsx` — extraction candidates: `EventComponent`, `MetricCard`, `FilterPill`, `BookingFormDialog`, `CalendarToolbar`
- (Add others as observed during the diagnosis pass)
```

This skeleton is committed BEFORE the human runs the measurements (next task fills in the Baseline numbers). Strategy Decision and Post-Wave-2 sections remain empty for now — Plan 03 fills Strategy Decision; Plan 03 Task 3 fills Post-Wave-2.
  </action>
  <verify>
    <automated>test -f .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md &amp;&amp; grep -c "^| 75%\|^| 100%\|^| 125%" .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md` exists
    - `grep -c "^| 75%\|^| 100%\|^| 125%" 20-DIAGNOSIS.md` returns at least `27` (3 tables × 9 rows each — Baseline, Post-Wave-1, Post-Wave-2)
    - `grep "## Baseline" 20-DIAGNOSIS.md` returns 1 hit
    - `grep "## Post-Wave-1" 20-DIAGNOSIS.md` returns 1 hit
    - `grep "## Strategy Decision" 20-DIAGNOSIS.md` returns 1 hit
    - `grep "## Post-Wave-2" 20-DIAGNOSIS.md` returns 1 hit
    - `grep "getBoundingClientRect" 20-DIAGNOSIS.md` returns 1 hit (snippet embedded)
    - `grep "Strategy A\|Strategy B\|Strategy C\|Strategy D" 20-DIAGNOSIS.md` returns at least 4 hits (decision rule documented)
  </acceptance_criteria>
  <done>
20-DIAGNOSIS.md skeleton exists with: embedded DevTools snippet, three empty 9-row tables (Baseline + Post-Wave-1 + Post-Wave-2), Strategy Decision section with the four-strategy decision rule copied from RESEARCH.md, and a Structural Debt section ready for diagnosis observations. Numbers are unfilled — next task fills Baseline.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run baseline diagnostic measurement at 9-cell matrix and record in 20-DIAGNOSIS.md § Baseline</name>
  <files>.planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md</files>
  <read_first>
    - .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md (just-created skeleton — has the snippet ready to paste)
    - .planning/phases/20-calendar-timeline-structure-audit/20-RESEARCH.md (§ Diagnostic Snippet, lines 318–342; § Suspect Hot Spots, lines 600–616 — for observation context)
    - .planning/phases/20-calendar-timeline-structure-audit/20-CONTEXT.md (D-01, D-05)
    - node_modules/react-big-calendar/lib/css/react-big-calendar.css (reference only — confirms label DOM shape if measurer wants to inspect)
  </read_first>
  <action>
Manual browser verification task — see <how-to-verify> below for the exact procedure. Claude cannot automate browser DevTools, browser zoom, or visual inspection; the human executor performs the steps and records results in the file listed in <files>. The orchestrator must surface the <how-to-verify> instructions to the human and wait for the resume-signal.
  </action>
  <what-built>
20-DIAGNOSIS.md skeleton + 20-HUMAN-UAT.md exist on disk. The skeleton has the DevTools snippet embedded and 9 empty rows in the Baseline table waiting for `MAX offset` / `MEAN offset` / `Pass/Fail` numbers.
  </what-built>
  <how-to-verify>
This is a **manual browser task** — Claude cannot automate browser DevTools or browser zoom. The human runs the measurements and writes the numbers. Steps:

1. Run `npm run dev` (port 5000) and open the admin calendar at http://localhost:5000/admin (login if needed; navigate to the Calendar / Planning Desk section).
2. Open DevTools Console (F12 → Console tab).
3. Copy the DevTools snippet from `20-DIAGNOSIS.md § DevTools Snippet` (or from `20-RESEARCH.md` lines 322–339).
4. For each of the 9 (zoom × view) cells, in order:
   - Set browser zoom (Ctrl/Cmd + +/- to reach 75%, 100%, or 125%; visible in browser UI).
   - Switch the calendar view (toolbar buttons: Day, Week, Month — for "Day + ByStaff" use Day view + click "By Staff" toggle).
   - Wait for the calendar to settle (no spinner).
   - Paste the snippet into Console; press Enter.
   - Read the final two console lines: `MAX offset: X.XXpx` and `MEAN offset: X.XXpx`.
   - Open `20-DIAGNOSIS.md` and fill the corresponding row of the **## Baseline** table:
     - `MAX offset` column: e.g., `0.50px`
     - `MEAN offset` column: e.g., `0.12px`
     - `Pass/Fail` column: `PASS` if `|MAX| ≤ 1.0`, else `FAIL`
     - `Notes` column (optional): any visible artifact (e.g., "label visibly drifts down past 5 PM")

5. After all 9 cells are filled, write a 1–3 sentence summary in **### Baseline observations**:
   - Which cells failed? (list zoom × view combos)
   - Which suspect hot spot from RESEARCH.md § Suspect Hot Spots (1–6) does the symptom most closely match?
   - Any non-alignment artifacts noticed (e.g., flicker on filter toggle → suspects #2/#6; drift only at non-100% zoom → suspect #5)?

6. Commit the filled file with message: `docs(phase-20): record Wave 0 baseline diagnosis`.

The matrix order (3 zoom × 3 view = 9 cells) — paste the snippet 9 times. Browser zoom keyboard shortcuts: Windows = `Ctrl + +`, `Ctrl + -`, `Ctrl + 0` for reset. The calendar must remain mounted while measuring (do not refresh between cells if avoidable — though refresh is fine).

**Expected outcome:** All 9 rows of the **## Baseline** table contain MAX/MEAN/Pass-Fail values; the **### Baseline observations** subsection has at least one sentence describing what was seen.
  </how-to-verify>
  <verify>
    <automated>grep -c "PASS\|FAIL" .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PASS\|FAIL" 20-DIAGNOSIS.md` returns at least `9` (one Pass/Fail marker per Baseline row)
    - `grep -E "[0-9]+\.[0-9]+\s*px" 20-DIAGNOSIS.md` returns at least `18` hits (9 rows × MAX + MEAN columns)
    - `### Baseline observations` section contains at least one non-template sentence (i.e., line under the heading is not just `(To fill in...)`)
    - File frontmatter `last_updated` field updated to today's date
  </acceptance_criteria>
  <resume-signal>Type "baseline-recorded" when all 9 rows of the Baseline table are filled and observations are written; or describe blockers (e.g., "calendar fails to render", "snippet returns no labels").</resume-signal>
  <done>
20-DIAGNOSIS.md § Baseline contains 9 rows of measurement data (MAX/MEAN/Pass-Fail per zoom × view), and § Baseline observations contains a written summary attributing observed symptoms to RESEARCH.md suspect hot spots. The phase now has a measurement floor against which Wave 1 can be compared.
  </done>
</task>

</tasks>

<verification>
- Run `test -f .planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md`
- Run `test -f .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md`
- Run `grep -c "^### CAL-FIX-" .planning/phases/20-calendar-timeline-structure-audit/20-HUMAN-UAT.md` → must return 4
- Run `grep -c "PASS\|FAIL" .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md` → must return ≥ 9 (Baseline filled)
</verification>

<success_criteria>
- 20-HUMAN-UAT.md exists with 4 entries (verbatim pass criteria from VALIDATION.md)
- 20-DIAGNOSIS.md exists with skeleton + filled Baseline (9 cells)
- Baseline observations cite at least one RESEARCH.md suspect hot spot
- Both files committed to git
- Wave 1 (Plan 02) can begin with a known measurement floor
</success_criteria>

<output>
After completion, create `.planning/phases/20-calendar-timeline-structure-audit/20-01-SUMMARY.md` documenting:
- Files created (paths)
- Baseline measurement summary (e.g., "Day/Week passing at all zooms; Day+ByStaff failing at 75% by ~3px")
- Suspect attribution (which of the 6 hot spots best explains the symptoms)
- Whether Plan 02 can proceed without modification, OR whether Wave 1 task list needs adjustment based on findings
</output>
