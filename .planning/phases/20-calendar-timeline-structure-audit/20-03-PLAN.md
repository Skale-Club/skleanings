---
phase: 20-calendar-timeline-structure-audit
plan: 03
type: execute
wave: 2
depends_on: [20-02]
files_modified:
  - .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md
  - client/src/index.css
autonomous: false
requirements: [CAL-FIX-01, CAL-FIX-03]
must_haves:
  truths:
    - "20-DIAGNOSIS.md § Strategy Decision is filled with a Selected strategy (A/B/C/D) and a Justification citing specific Post-Wave-1 measurements"
    - "If Strategy A is selected: client/src/index.css is unchanged (no-op task) and the rationale is documented"
    - "If Strategy B/C/D is selected: client/src/index.css is updated with the EXACT CSS block copied from RESEARCH.md § Pixel-Alignment Strategies; `npm run check && npm run build` passes"
    - "Post-Wave-2 measurements are recorded in 20-DIAGNOSIS.md § Post-Wave-2 with delta vs Post-Wave-1"
    - "All 9 Post-Wave-2 cells PASS (MAX |offset| ≤ 1px) — OR — escalation note exists pointing to Wave 3 conditional remount task"
  artifacts:
    - path: ".planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md"
      provides: "Strategy Decision filled; Post-Wave-2 measurement table filled"
      contains: "Selected strategy:"
    - path: "client/src/index.css"
      provides: "CSS strategy applied (or unchanged for Strategy A)"
      contains_one_of:
        - "transform: translateY(-50%)"
        - "margin-top: -0.5em"
        - ".rbc-time-slot:first-child"
        - "min-height: 2.25rem"
  key_links:
    - from: "20-DIAGNOSIS.md § Post-Wave-1"
      to: "20-DIAGNOSIS.md § Strategy Decision"
      via: "Strategy choice cites a specific measurement pattern"
      pattern: "Selected strategy"
    - from: "20-DIAGNOSIS.md § Strategy Decision"
      to: "client/src/index.css `.rbc-label` rules"
      via: "CSS block matches the verbatim code from RESEARCH.md for the chosen strategy"
      pattern: "appointments-calendar .rbc-time-gutter .rbc-label"
---

<objective>
Wave 2 — Diagnosis-driven CSS strategy selection. Read the Post-Wave-1 measurements from `20-DIAGNOSIS.md`, apply the decision rule from RESEARCH.md § Pixel-Alignment Strategies (lines 484–488) to select ONE of Strategies A/B/C/D, then apply that strategy to `client/src/index.css` (or document a no-op if Strategy A wins). Re-measure to confirm.

Purpose: Per CONTEXT.md D-03 (root-cause over surface-fix), the CSS choice MUST be driven by measurement evidence, not guessed. RESEARCH.md provides four candidate strategies with explicit decision criteria — this plan executes the choice.

Output:
- `.planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md` § Strategy Decision filled
- `client/src/index.css` updated (or documented unchanged for Strategy A)
- `.planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md` § Post-Wave-2 filled
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-calendar-timeline-structure-audit/20-CONTEXT.md
@.planning/phases/20-calendar-timeline-structure-audit/20-RESEARCH.md
@.planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md
@.planning/phases/20-calendar-timeline-structure-audit/20-02-SUMMARY.md
@client/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read Post-Wave-1 measurements and select CSS strategy (A/B/C/D); fill 20-DIAGNOSIS.md § Strategy Decision</name>
  <files>.planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md</files>
  <read_first>
    - .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md (§ Baseline + § Post-Wave-1 — the data driving the decision; § Strategy Decision — the section to fill)
    - .planning/phases/20-calendar-timeline-structure-audit/20-RESEARCH.md (§ Pixel-Alignment Strategies for `.rbc-label`, lines 418–488 — verbatim decision rule)
    - .planning/phases/20-calendar-timeline-structure-audit/20-CONTEXT.md (D-03 root-cause over surface-fix)
  </read_first>
  <action>
Read the **## Post-Wave-1** table from `20-DIAGNOSIS.md`. Apply the decision rule from RESEARCH.md § Pixel-Alignment Strategies (copied verbatim below — this is the rule, do not derive it from scratch):

**Decision rule (from RESEARCH.md lines 484–488):**
- **Strategy A — keep Phase 18 transform (no CSS change):** All 9 Post-Wave-1 cells show `MAX |offset| ≤ 1.0 px`. Wave 1's memoization fixes resolved the misalignment; Phase 18's `transform: translateY(-50%)` is sufficient.
- **Strategy B — flex anchor + negative margin:** Diagnosis shows transform-related drift inside sticky parent (drift visible only in By Staff view, particularly at non-100% zoom — pattern suggests sticky × transform interaction).
- **Strategy C — absolute position inside first slot:** Offset only in By Staff view, OR diagnosis shows offset varies between views in a way that points to the slot's flex height changing per resource column.
- **Strategy D — match gutter slot heights to day-slot override (Pitfall 5):** Uniform offset at 100% but drift at 75% / 125% — indicates `min-height: 2.25rem` on day-slots vs `40px` group min-height on gutter rounding differently at non-integer zoom.

**Tie-breaker:** If multiple strategies fit, prefer the strategy that touches FEWER CSS rules (A < D < B < C). Per CONTEXT.md D-02, narrow scope wins.

Open `20-DIAGNOSIS.md` and:

1. Fill `**Selected strategy:**` field under § Strategy Decision with one of: `Strategy A`, `Strategy B`, `Strategy C`, `Strategy D`.
2. Fill `**Justification:**` with at least 2 sentences citing specific Post-Wave-1 measurements (e.g., "Cells `75% × Day+ByStaff` and `125% × Day+ByStaff` show MAX 2.3px and 2.1px respectively; all other cells pass. Pattern matches Strategy C decision criterion (offset only in By Staff view).").
3. If Strategy A is selected: ALSO add a line `**Plan 03 Task 2 expected to be a no-op (no CSS edit).** Plan 03 Task 3 will re-measure to confirm.`
4. Update frontmatter `last_updated` to today's date.

Commit with message: `docs(phase-20): select CSS strategy {N} from Post-Wave-1 diagnosis`.
  </action>
  <verify>
    <automated>grep "Selected strategy: Strategy [ABCD]" .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Selected strategy: Strategy [ABCD]" 20-DIAGNOSIS.md` returns exactly 1 hit
    - `grep -A 1 "Justification:" 20-DIAGNOSIS.md` returns at least 1 line of justification text (not blank)
    - Justification mentions at least one specific zoom/view cell (regex: `(75%|100%|125%)` and `(Day|Week|ByStaff|By Staff)` both appear in the justification block)
    - File frontmatter `last_updated` updated to today's date
  </acceptance_criteria>
  <done>
20-DIAGNOSIS.md § Strategy Decision contains a clear Selected strategy + Justification cross-referencing Post-Wave-1 cells. Task 2 below has all four strategy code blocks ready and picks based on this decision.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply selected CSS strategy to client/src/index.css (or no-op for Strategy A)</name>
  <files>client/src/index.css</files>
  <read_first>
    - client/src/index.css (lines 226–428 — all calendar CSS; lines 363–365 day-slot min-height; lines 388–394 current `.rbc-label` rule)
    - .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md (§ Strategy Decision — just filled by previous task)
    - .planning/phases/20-calendar-timeline-structure-audit/20-RESEARCH.md (§ Pixel-Alignment Strategies — verbatim CSS blocks for each strategy, lines 422–482)
    - .planning/phases/20-calendar-timeline-structure-audit/20-CONTEXT.md (D-02 narrow scope, D-03 root-cause)
    - node_modules/react-big-calendar/lib/css/react-big-calendar.css (reference only — confirms cascade if needed)
  </read_first>
  <action>
Read `20-DIAGNOSIS.md § Strategy Decision` to determine the Selected strategy. Then apply ONLY that strategy's CSS block. All four strategy blocks are listed below verbatim — pick the matching one.

The current rule at `client/src/index.css` lines 388–394 is:
```css
.appointments-calendar .rbc-time-gutter .rbc-label {
  display: block;
  padding-top: 0;
  line-height: 1;
  transform: translateY(-50%);
  overflow: visible;
}
```

--- 

**If Selected strategy = Strategy A:** Do NOT modify `client/src/index.css`. Skip to verification. The expected acceptance is "file unchanged but type check + build still pass" — this is a deliberate no-op task.

--- 

**If Selected strategy = Strategy B (Flex anchor + negative margin):**
REPLACE lines 388–394 of `client/src/index.css` with:
```css
.appointments-calendar .rbc-time-gutter .rbc-label {
  display: block;
  line-height: 1;
  margin-top: -0.5em;
  padding-top: 0;
  overflow: visible;
}
```
Then test the FIRST hour row carefully — Strategy B has a known caveat that the first label can clip if it sits at the gutter top edge. If the first label clips, add a single-rule mitigation:
```css
.appointments-calendar .rbc-time-gutter .rbc-timeslot-group:first-child .rbc-label {
  margin-top: 0;
}
```

--- 

**If Selected strategy = Strategy C (Absolute position inside first slot):**
REPLACE lines 388–394 of `client/src/index.css` with:
```css
.appointments-calendar .rbc-time-gutter .rbc-time-slot:first-child {
  position: relative;
}
.appointments-calendar .rbc-time-gutter .rbc-time-slot:first-child .rbc-label {
  position: absolute;
  top: 0;
  transform: translateY(-50%);
  left: 5px;
  right: 5px;
  display: block;
  line-height: 1;
}
```

--- 

**If Selected strategy = Strategy D (Match gutter slot heights to day-slot override — Pitfall 5):**
KEEP lines 388–394 unchanged (Phase 18's transform stays). ADD a new rule immediately after the existing `.rbc-time-view .rbc-day-slot .rbc-time-slot { min-height: 2.25rem; }` rule (currently around line 363–365). The new rule:
```css
.appointments-calendar .rbc-time-view .rbc-time-gutter .rbc-time-slot {
  min-height: 2.25rem;
}
```
This matches the gutter slot height to the day-slot override, eliminating the rounding mismatch at non-integer zoom levels.

--- 

**Constraints (apply to all strategies):**
- Stay inside the existing `.appointments-calendar` cascade — do NOT introduce a new top-level selector.
- Do NOT remove the existing `.rbc-time-view .rbc-day-slot .rbc-time-slot { min-height: 2.25rem; }` rule (lines 363–365) — RESEARCH.md notes this could be removed as an alternative for Strategy D, but per CONTEXT.md D-02 narrow scope, prefer adding the matching gutter rule (less risk to day-slot rendering).
- After the edit, run `npm run check` (no JS changes — should pass trivially) and `npm run build` (verifies CSS still parses).
- Do NOT touch any other CSS rule. Do NOT clean up unrelated rules. This is the narrow scope.
- Commit with message: `fix(phase-20): apply CSS Strategy {N} for `.rbc-label` alignment` referencing 20-DIAGNOSIS.md.
  </action>
  <verify>
    <automated>npm run check &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0
    - `npm run build` exits 0
    - If Strategy A: `git diff --stat client/src/index.css` shows 0 lines changed (file unchanged); 20-DIAGNOSIS.md notes the no-op
    - If Strategy B: `grep "margin-top: -0.5em" client/src/index.css` returns 1 hit; `grep "transform: translateY(-50%)" client/src/index.css` returns 0 hits inside the `.rbc-time-gutter .rbc-label` rule (replaced)
    - If Strategy C: `grep ".rbc-time-gutter .rbc-time-slot:first-child" client/src/index.css` returns at least 1 hit; `grep "position: absolute" client/src/index.css` returns at least 1 hit in the calendar block
    - If Strategy D: `grep -c "min-height: 2.25rem" client/src/index.css` returns at least `2` (existing day-slot rule + new gutter-slot rule); `grep "rbc-time-gutter .rbc-time-slot" client/src/index.css` returns at least 1 hit
    - File frontmatter / cascade structure intact (no orphan braces — `npm run build` would catch this but be defensive)
  </acceptance_criteria>
  <done>
The selected CSS strategy is applied (or deliberately not applied for Strategy A). Type check and build both pass. Ready for Post-Wave-2 measurement.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Re-run diagnostic measurement after CSS strategy applied; record in 20-DIAGNOSIS.md § Post-Wave-2; flag Wave 3 escalation if needed</name>
  <files>.planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md</files>
  <read_first>
    - .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md (§ Post-Wave-1 to compute Δ; § Strategy Decision for context; § Post-Wave-2 to fill)
    - .planning/phases/20-calendar-timeline-structure-audit/20-RESEARCH.md (§ Diagnostic Snippet, lines 318–342; § Architecture Patterns 4 — `key={view-isByStaff}` escalation, lines 236–249)
  </read_first>
  <action>
Manual browser verification task — see <how-to-verify> below for the exact procedure. Claude cannot automate browser DevTools, browser zoom, or visual inspection; the human executor performs the steps and records results in the file listed in <files>. The orchestrator must surface the <how-to-verify> instructions to the human and wait for the resume-signal.
  </action>
  <what-built>
The selected CSS strategy from Plan 03 Task 1 is applied (or deliberately not applied for Strategy A). The calendar should now have either: (a) no visible change (Strategy A — no-op confirms Wave 1 alone fixed it), or (b) tightened alignment matching the chosen strategy. Type check + build both pass.
  </what-built>
  <how-to-verify>
**Manual browser task** — same 9-cell measurement matrix as Wave 0 / Post-Wave-1. Steps:

1. Run `npm run dev` (port 5000) — restart to pick up CSS changes (Vite HMR usually handles this; restart if uncertain).
2. Open admin calendar.
3. Open DevTools Console.
4. For each of the 9 (zoom × view) cells:
   - Set zoom (75%, 100%, 125%).
   - Switch view (Day, Week, Day + ByStaff).
   - Wait for calendar to settle.
   - Paste the snippet from `20-DIAGNOSIS.md § DevTools Snippet`.
   - Read MAX/MEAN.
   - Fill the corresponding row of **## Post-Wave-2** table:
     - `MAX offset`, `MEAN offset`, `Δ vs Post-Wave-1`, `Pass/Fail` (PASS if `|MAX| ≤ 1.0`).

5. Write **### Post-Wave-2 observations** (1–3 sentences):
   - Did the CSS strategy resolve remaining offsets?
   - Are all 9 cells now PASS?
   - If any cells still FAIL: which? What's the residual pattern?

6. **Wave 3 escalation routing (informational — affects Plan 04 only):**
   - **All 9 cells PASS:** Plan 04 proceeds with no remount task. Note in observations: "All cells passing — Wave 3 conditional `key={view-isByStaff}` remount NOT needed."
   - **Some cells still FAIL:** Note: "Residual offsets > 1px in cells [list]. Plan 04 should activate the conditional `key={currentView}-${isByStaff}` remount task and re-measure once more."
   - **CAL-FIX-02 view-switch staleness suspected:** During measurement, also walk the D-06 sequence (Month → Week → Day → By Staff → Week → Month) once and note any visible stale layout — if observed, Plan 04's conditional remount task should also activate.

7. Commit: `docs(phase-20): record Post-Wave-2 diagnosis after CSS strategy {N}`.

**Expected outcome:** All 9 rows of **## Post-Wave-2** filled; observations include explicit guidance for Plan 04 (whether to activate the conditional remount task).
  </how-to-verify>
  <verify>
    <automated>grep -A 20 "## Post-Wave-2" .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md | grep -c "PASS\|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Inside the `## Post-Wave-2` section, at least `9` PASS/FAIL markers exist
    - At least 27 numeric `[-+]?[0-9]+\.[0-9]+\s*px` entries inside the Post-Wave-2 table block (9 rows × 3 numeric columns)
    - `### Post-Wave-2 observations` section contains explicit guidance for Plan 04 (regex match for either `Wave 3 conditional remount NOT needed` OR `Wave 3.* should activate` OR similar — the executor's exact wording will vary, but the guidance must be present)
    - `npm run check` exits 0 (no regression introduced)
  </acceptance_criteria>
  <resume-signal>Type "post-wave-2-recorded" with one of: "all-pass-no-remount" or "residual-needs-remount" or "needs-discussion" if findings are ambiguous.</resume-signal>
  <done>
20-DIAGNOSIS.md § Post-Wave-2 contains 9 measurement rows + observations. Plan 04 has explicit routing instructions for the conditional remount task.
  </done>
</task>

</tasks>

<verification>
- Run `grep "Selected strategy: Strategy" .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md` — must return 1 hit
- Run `npm run check` — must pass
- Run `npm run build` — must pass
- Run `grep -A 20 "## Post-Wave-2" .planning/phases/20-calendar-timeline-structure-audit/20-DIAGNOSIS.md | grep -c "PASS\|FAIL"` — must return ≥ 9
</verification>

<success_criteria>
- CSS strategy selected based on Post-Wave-1 evidence
- Selected strategy applied (or no-op for Strategy A)
- Type check + build pass
- Post-Wave-2 measurements recorded
- Plan 04 routing guidance written in observations
</success_criteria>

<output>
After completion, create `.planning/phases/20-calendar-timeline-structure-audit/20-03-SUMMARY.md` documenting:
- Selected strategy + justification
- CSS edits made (line numbers, before/after)
- Post-Wave-2 measurement summary
- Recommendation for Plan 04 (activate remount task or skip it)
</output>
