---
phase: 39-storage-refactor
plan: "03"
type: execute
wave: 3
depends_on:
  - "39-01"
  - "39-02"
files_modified:
  - server/storage.ts
autonomous: true
requirements:
  - MT-07

must_haves:
  truths:
    - "Every remaining method group (Chat, Conversations, Blog, Staff, Contacts, Notifications, Recurring Bookings, Calendar Sync Queue) includes tenantId filter on all reads and writes"
    - "The three raw SQL methods (getCalendarSyncHealth, retryCalendarSyncJob, listRecentSyncFailures) include AND tenant_id = ${this.tenantId} in their SQL strings"
    - "upsertContact scopes the email uniqueness check to this.tenantId so tenant 1 and tenant 2 can share the same email address"
    - "No business query method in the entire storage.ts file is missing a tenantId filter"
    - "npm run check passes with zero errors after all edits"
  artifacts:
    - path: server/storage.ts
      provides: "Fully tenant-scoped DatabaseStorage — every method group updated"
      contains: "AND tenant_id = ${this.tenantId}"
  key_links:
    - from: "getCalendarSyncHealth()"
      to: "AND tenant_id = ${this.tenantId}"
      via: "raw SQL template literal"
      pattern: "AND tenant_id = \\$\\{this\\.tenantId\\}"
    - from: "upsertContact()"
      to: "eq(contacts.tenantId, this.tenantId)"
      via: "email uniqueness WHERE clause"
      pattern: "eq\\(contacts\\.tenantId, this\\.tenantId\\)"
    - from: "getRecurringBookingsWithDetails()"
      to: "eq(recurringBookings.tenantId, this.tenantId)"
      via: ".where() on driving table"
      pattern: "eq\\(recurringBookings\\.tenantId, this\\.tenantId\\)"
---

<objective>
Apply tenant filtering to the remaining method groups in DatabaseStorage — Chat, Conversations, Blog, Staff, Contacts, Notifications, Recurring Bookings, and Calendar Sync Queue (raw SQL) — completing MT-07 so that no business query in storage.ts reads or writes rows without a tenantId filter.

Purpose: After Plans 01 and 02, the high-traffic core is scoped. This plan handles the remaining ~45 methods including the three raw SQL queries (highest-risk pitfall) and the upsertContact email uniqueness bug. When this plan completes, the full MT-07 requirement is satisfied.

Output: server/storage.ts fully refactored. `npm run check` green. Every `db.select()`, `db.insert()`, `db.update()`, `db.delete()`, and `db.execute(sql`...`)` in the class body includes `this.tenantId` in its WHERE or VALUES.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-storage-refactor/39-RESEARCH.md
@.planning/phases/39-storage-refactor/39-02-SUMMARY.md
</context>

<interfaces>
<!-- Raw SQL pattern from RESEARCH.md Pattern 7 -->
<!-- Before: -->
```sql
WHERE target = ${target} AND status = 'pending'
```
<!-- After: -->
```sql
WHERE target = ${target} AND status = 'pending'
  AND tenant_id = ${this.tenantId}
```

<!-- upsertContact pattern from RESEARCH.md Pattern 8 -->
<!-- After: -->
```typescript
const [existing] = await db.select().from(contacts)
  .where(and(
    eq(contacts.tenantId, this.tenantId),
    eq(contacts.email, data.email)
  ));
```

<!-- JOIN driving table pattern from RESEARCH.md Pattern 6 -->
```typescript
// getRecurringBookingsWithDetails — filter on the driving table
.where(eq(recurringBookings.tenantId, this.tenantId))
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply tenantId to Chat, Conversations, Blog Posts, Blog Settings, Blog Generation Jobs, Time Slot Locks, Staff methods</name>
  <files>server/storage.ts</files>

  <read_first>
    - server/storage.ts (Chat, Conversations, Blog, Staff sections — read the actual current code for each group before editing)
    - .planning/phases/39-storage-refactor/39-RESEARCH.md (Method Audit Summary table — groups: Chat, Blog, Staff)
  </read_first>

  <action>
Edit server/storage.ts for the following groups. Run `npm run check` after all groups in this task.

**Group: Chat Settings + Chat Integrations** (singleton pattern)
Tables: `chatSettings`, `chatIntegrations`

- `getChatSettings()`:
  - SELECT path: add `.where(eq(chatSettings.tenantId, this.tenantId))`
  - INSERT fallback: add `tenantId: this.tenantId` to values
- `updateChatSettings(id, settings)`: Wrap WHERE with `and(eq(chatSettings.tenantId, this.tenantId), eq(chatSettings.id, id))`
- `getChatIntegration(provider)`: Add `and(eq(chatIntegrations.tenantId, this.tenantId), eq(chatIntegrations.provider, provider))`
- `upsertChatIntegration(integration)`: Add `tenantId: this.tenantId` to values; if conflict target is `(provider)`, it must also be scoped to tenant (note: this may require `(tenant_id, provider)` conflict target — if the table only has a `provider` unique constraint, just spread tenantId into values and use `onConflictDoUpdate`)

**Group: Twilio / Telegram / Email Settings** (singleton pattern per table)
Tables: `twilioSettings`, `telegramSettings`, `emailSettings`

- `getTwilioSettings()`: Add `.where(eq(twilioSettings.tenantId, this.tenantId))`; INSERT fallback gets `tenantId: this.tenantId`
- `saveTwilioSettings(settings)`: SELECT and UPDATE paths get tenant filter; INSERT path gets `tenantId: this.tenantId`
- Same pattern for `getTelegramSettings`, `saveTelegramSettings`, `getEmailSettings`, `saveEmailSettings`

**Group: Conversations + Conversation Messages**
Tables: `conversations`, `conversationMessages`

- `getConversations()`: Add `.where(eq(conversations.tenantId, this.tenantId))`
- `getConversation(id)`: Add `and(eq(conversations.tenantId, this.tenantId), eq(conversations.id, id))`
- `updateConversation(id, data)`: Wrap WHERE
- `deleteConversation(id)`: Wrap WHERE
- `createConversation(data)`: Spread `tenantId: this.tenantId` into values
- `addConversationMessage(data)`: Spread `tenantId: this.tenantId` into values for `conversationMessages`
- `getConversationMessages(conversationId)`: Add `and(eq(conversationMessages.tenantId, this.tenantId), eq(conversationMessages.conversationId, conversationId))`
- `findOpenConversationByContact(contactId)`: Add tenant filter to WHERE

**Group: Blog Posts**
Tables: `blogPosts`, `blogPostServices`

- `getBlogPosts()`: Add `.where(eq(blogPosts.tenantId, this.tenantId))`
- `getBlogPost(id)`: Add tenant filter
- `getBlogPostBySlug(slug)`: Add `and(eq(blogPosts.tenantId, this.tenantId), eq(blogPosts.slug, slug))`
- `getPublishedBlogPosts()`: Prepend tenant filter to existing conditions
- `getRelatedBlogPosts(serviceId, excludeId)`: Prepend tenant filter
- `createBlogPost(post)`: Spread `tenantId: this.tenantId` into values
- `updateBlogPost(id, post)`: Wrap WHERE
- `deleteBlogPost(id)`: Wrap WHERE
- `getBlogPostServices(postId)`: Add `and(eq(blogPostServices.tenantId, this.tenantId), eq(blogPostServices.blogPostId, postId))`
- `setBlogPostServices(postId, serviceIds)`: DELETE WHERE → `and(eq(blogPostServices.tenantId, this.tenantId), eq(blogPostServices.blogPostId, postId))`; INSERT values: each object gets `tenantId: this.tenantId`
- `countPublishedBlogPosts()`: Add `.where(and(eq(blogPosts.tenantId, this.tenantId), eq(blogPosts.status, 'published')))`

**Group: Blog Settings** (singleton)
Tables: `blogSettings`

- `getBlogSettings()`: SELECT path + INSERT fallback both scoped to `this.tenantId`
- `upsertBlogSettings(settings)`: Add `tenantId: this.tenantId` to values

**Group: Blog Generation Jobs**
Tables: `blogGenerationJobs`

- `getBlogGenerationJobs()`: Add tenant filter
- `getBlogGenerationJob(id)`: Add tenant filter
- `createBlogGenerationJob(job)`: Spread `tenantId: this.tenantId`
- `updateBlogGenerationJob(id, data)`: Wrap WHERE
- `acquireBlogGenerationLock(jobId, lockerId)`: Wrap WHERE
- `releaseBlogGenerationLock(jobId, lockerId)`: Wrap WHERE

**Group: Time Slot Locks**
Tables: `timeSlotLocks`

- `acquireTimeSlotLock(slotKey, lockerId, ttlSeconds)`: SELECT path and INSERT path both get tenant filter
- `releaseTimeSlotLock(slotKey, lockerId)`: Wrap WHERE with `and(eq(timeSlotLocks.tenantId, this.tenantId), ...existing conditions)`
- `cleanExpiredTimeSlotLocks()`: Add `.where(and(eq(timeSlotLocks.tenantId, this.tenantId), lte(timeSlotLocks.expiresAt, new Date())))`

**Group: Staff Members + Staff Service Abilities + Staff Availability + Staff Availability Overrides + Staff Google Calendar**
Tables: `staffMembers`, `staffServiceAbilities`, `staffAvailability`, `staffAvailabilityOverrides`, `staffGoogleCalendar`

- All SELECT methods: add `eq(staffMembers.tenantId, this.tenantId)` (or the appropriate table)
- All INSERT methods: spread `tenantId: this.tenantId`
- All UPDATE/DELETE methods: wrap WHERE with `and(eq(table.tenantId, this.tenantId), ...)`
- `getStaffMembersByService(serviceId)`: This JOINs staffServiceAbilities + staffMembers; filter on the driving table `staffServiceAbilities` with `eq(staffServiceAbilities.tenantId, this.tenantId)`
- `getServicesByStaffMember(staffMemberId)`: Filter driving table
- `getStaffMembersByServiceId(serviceId)`: Filter driving table
- `setStaffServiceAbilities(staffMemberId, serviceIds)`: DELETE WHERE → `and(eq(staffServiceAbilities.tenantId, this.tenantId), eq(staffServiceAbilities.staffMemberId, staffMemberId))`; INSERT values: each object gets `tenantId: this.tenantId`
- `getAllCalendarStatuses()`: Add `.where(eq(staffGoogleCalendar.tenantId, this.tenantId))`

After all groups, run `npm run check` and fix any TypeScript errors.
  </action>

  <verify>
    <automated>cd /c/Users/Vanildo/Dev/skleanings && npm run check 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `grep "eq(chatSettings.tenantId, this.tenantId)" server/storage.ts` returns at least 2 matches
    - `grep "eq(conversations.tenantId, this.tenantId)" server/storage.ts` returns at least 3 matches
    - `grep "eq(blogPosts.tenantId, this.tenantId)" server/storage.ts` returns at least 5 matches
    - `grep "eq(staffMembers.tenantId, this.tenantId)" server/storage.ts` returns at least 4 matches
    - `grep "eq(staffServiceAbilities.tenantId, this.tenantId)" server/storage.ts` returns at least 2 matches
    - `grep "eq(timeSlotLocks.tenantId, this.tenantId)" server/storage.ts` returns at least 2 matches
    - `npm run check` exits with code 0
  </acceptance_criteria>

  <done>Chat, Conversations, Blog, Staff, Time Slot Locks method groups fully tenant-scoped. TypeScript compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply tenantId to Contacts, Notification Logs, Recurring Bookings, and Calendar Sync Queue (raw SQL)</name>
  <files>server/storage.ts</files>

  <read_first>
    - server/storage.ts (Contacts, Notification Logs, Recurring Bookings, Calendar Sync Queue sections — read the actual current code)
    - .planning/phases/39-storage-refactor/39-RESEARCH.md (Pattern 7 for raw SQL; Pattern 8 for upsertContact; Pattern 6 for JOIN methods; Pitfall 5 for raw SQL; Pitfall 4 for JOIN tables)
  </read_first>

  <action>
Edit server/storage.ts for the final method groups.

**Group: Contacts**
Tables: `contacts`

- `getContact(id)`: Add `and(eq(contacts.tenantId, this.tenantId), eq(contacts.id, id))`
- `listContactsWithStats(...)`: Prepend `eq(contacts.tenantId, this.tenantId)` to all WHERE conditions
- `upsertContact(data)`:
  - The email uniqueness check SELECT must be: `.where(and(eq(contacts.tenantId, this.tenantId), eq(contacts.email, data.email)))`
  - The INSERT values must include `tenantId: this.tenantId`
  - If an UPDATE path exists, wrap its WHERE with `and(eq(contacts.tenantId, this.tenantId), eq(contacts.id, existing.id))`
- `updateContact(id, data)`: Wrap WHERE with `and(eq(contacts.tenantId, this.tenantId), eq(contacts.id, id))`
- `updateBookingContactId(bookingId, contactId)`: This updates `bookings` — wrap WHERE with `and(eq(bookings.tenantId, this.tenantId), eq(bookings.id, bookingId))`
- `getContactBookings(contactId)`: Add `and(eq(bookings.tenantId, this.tenantId), eq(bookings.contactId, contactId))`

**Group: Notification Logs**
Tables: `notificationLogs`

- `createNotificationLog(entry)`: Spread `tenantId: this.tenantId` into values
- `getNotificationLogsByConversation(conversationId)`: Add `and(eq(notificationLogs.tenantId, this.tenantId), eq(notificationLogs.conversationId, conversationId))`
- `getNotificationLogsByBooking(bookingId)`: Add `and(eq(notificationLogs.tenantId, this.tenantId), eq(notificationLogs.bookingId, bookingId))`
- `getNotificationLogs(filters)`: Prepend `eq(notificationLogs.tenantId, this.tenantId)` to the conditions array

**Group: Recurring Bookings**
Tables: `recurringBookings`, joined with `contacts`, `services`

- `createRecurringBooking(data)`: Spread `tenantId: this.tenantId` into values
- `getRecurringBooking(id)`: Add `and(eq(recurringBookings.tenantId, this.tenantId), eq(recurringBookings.id, id))`
- `getRecurringBookings(statusFilter?)`: Prepend `eq(recurringBookings.tenantId, this.tenantId)` to conditions
- `getActiveRecurringBookingsDueForGeneration(asOfDate)`: Prepend `eq(recurringBookings.tenantId, this.tenantId)` to the `and(...)` call
- `updateRecurringBooking(id, data)`: Wrap WHERE with `and(eq(recurringBookings.tenantId, this.tenantId), eq(recurringBookings.id, id))`
- `getRecurringBookingByToken(token)`: Add `and(eq(recurringBookings.tenantId, this.tenantId), eq(recurringBookings.manageToken, token))`
- `getRecurringBookingsWithDetails()`: This JOINs `recurringBookings` with `contacts` and `services`. Add `.where(eq(recurringBookings.tenantId, this.tenantId))` filtering on the driving table `recurringBookings`

**Group: Calendar Sync Queue** (3 raw SQL methods — highest risk)
Tables: `calendarSyncQueue` (raw SQL with `db.execute(sql`...`)`)

Important: These use template literal SQL. The tenant_id column exists per Phase 38. Add `AND tenant_id = ${this.tenantId}` to every WHERE clause in all three methods.

- `enqueueCalendarSync(bookingId, target, operation, payload?)`:
  - This likely does a `db.insert(calendarSyncQueue).values(...)` — spread `tenantId: this.tenantId` into values

- `getCalendarSyncHealth()`:
  Read the actual current SQL. It queries `calendar_sync_queue` with WHERE clauses. Add `AND tenant_id = ${this.tenantId}` to EVERY SQL statement in this method. There are multiple queries (pending count, failed count, recent failures). Each must get the filter:
  ```sql
  WHERE target = ${target} AND status = 'pending'
    AND tenant_id = ${this.tenantId}
  ```
  ```sql
  WHERE target = ${target} AND status = 'failed_permanent'
    AND tenant_id = ${this.tenantId}
  ```
  ```sql
  WHERE target = ${target}
    AND tenant_id = ${this.tenantId}
  ORDER BY last_attempt_at DESC NULLS LAST
  LIMIT 20
  ```

- `retryCalendarSyncJob(jobId)`:
  Current SQL: `WHERE id = ${jobId} AND status IN (...)`. Add:
  `AND tenant_id = ${this.tenantId}`

- `listRecentSyncFailures(target?, limit)`:
  Current SQL has two branches (with target, without target). Both must add:
  `AND tenant_id = ${this.tenantId}`

After completing all groups, run the final `npm run check`. Fix any TypeScript errors.

Then run the exhaustive audit check:
```bash
# Count total this.tenantId references — should be well above 100
grep -c "this.tenantId" /c/Users/Vanildo/Dev/skleanings/server/storage.ts

# Verify no unfiltered db.select() remain (spot-check for large tables)
grep -n "db.select().from(bookings)" /c/Users/Vanildo/Dev/skleanings/server/storage.ts
grep -n "db.select().from(contacts)" /c/Users/Vanildo/Dev/skleanings/server/storage.ts
grep -n "db.select().from(services)" /c/Users/Vanildo/Dev/skleanings/server/storage.ts
```

For each result from the spot-check, confirm the query on that line has a `.where(...)` clause that includes `this.tenantId`. If any do not, add the filter immediately.
  </action>

  <verify>
    <automated>cd /c/Users/Vanildo/Dev/skleanings && npm run check 2>&1 | tail -20 && echo "---" && grep -c "this.tenantId" server/storage.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep "eq(contacts.tenantId, this.tenantId)" server/storage.ts` returns at least 4 matches (upsertContact SELECT, upsertContact INSERT/UPDATE, getContact, updateContact)
    - `grep "eq(recurringBookings.tenantId, this.tenantId)" server/storage.ts` returns at least 5 matches
    - `grep "eq(notificationLogs.tenantId, this.tenantId)" server/storage.ts` returns at least 3 matches
    - `grep "AND tenant_id = \${this.tenantId}" server/storage.ts` returns at least 6 matches (covering all raw SQL queries in getCalendarSyncHealth, retryCalendarSyncJob, listRecentSyncFailures)
    - `grep -c "this.tenantId" server/storage.ts` returns a number greater than 100
    - `npm run check` exits with code 0
    - The string `new DatabaseStorage()` (empty parens) does NOT appear in server/storage.ts
    - `grep "export const storage = DatabaseStorage.forTenant(1)" server/storage.ts` returns exactly 1 match
  </acceptance_criteria>

  <done>All remaining method groups (Contacts, Notification Logs, Recurring Bookings, Calendar Sync Queue raw SQL) are tenant-scoped. The upsertContact email uniqueness check is correctly scoped per tenant. Raw SQL methods include AND tenant_id = ${this.tenantId}. npm run check passes. MT-07 is fully satisfied — no business query in storage.ts reads or writes rows without a tenantId filter.</done>
</task>

</tasks>

<verification>
Phase 39 completion gate:

1. `npm run check` exits code 0
2. `grep -c "this.tenantId" server/storage.ts` returns > 100 (broad filter coverage)
3. `grep "export const storage = DatabaseStorage.forTenant(1)" server/storage.ts` returns 1 match
4. `grep "static forTenant(tenantId: number): DatabaseStorage" server/storage.ts` returns 1 match
5. `grep -rn "new DatabaseStorage()" --include="*.ts" . | grep -v node_modules` returns 0 matches
6. `grep "AND tenant_id = \${this.tenantId}" server/storage.ts` returns >= 6 matches
7. Manual smoke test: `npm run dev` starts without crash; /admin loads categories, services, bookings
</verification>

<success_criteria>
DatabaseStorage is fully tenant-scoped:
- MT-06: `DatabaseStorage.forTenant(id)` is the only public instantiation path
- MT-07: Every business query method includes the tenantId filter
- MT-08: `export const storage = DatabaseStorage.forTenant(1)` preserves backward compatibility — all existing routes work without modification
- `npm run check` green
- Admin dashboard smoke test confirms tenant 1 data loads correctly
</success_criteria>

<output>
After completion, create `.planning/phases/39-storage-refactor/39-03-SUMMARY.md`
</output>
