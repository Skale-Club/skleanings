---
phase: 39-storage-refactor
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - server/storage.ts
autonomous: true
requirements:
  - MT-06
  - MT-08

must_haves:
  truths:
    - "DatabaseStorage.forTenant(2) returns a new DatabaseStorage instance (type-safe, not `any`)"
    - "export const storage at the bottom of storage.ts is DatabaseStorage.forTenant(1), not new DatabaseStorage()"
    - "No call to `new DatabaseStorage()` exists anywhere in the codebase"
  artifacts:
    - path: server/storage.ts
      provides: "Class with private tenantId field, private constructor, static forTenant factory, updated singleton export"
      contains: "private readonly tenantId: number"
  key_links:
    - from: "DatabaseStorage.forTenant()"
      to: "private constructor"
      via: "static factory delegates to private constructor"
      pattern: "static forTenant\\(tenantId: number\\)"
    - from: "export const storage"
      to: "DatabaseStorage.forTenant(1)"
      via: "last line of file"
      pattern: "export const storage = DatabaseStorage\\.forTenant\\(1\\)"
---

<objective>
Introduce the static factory pattern into DatabaseStorage — the structural foundation that all subsequent tenant-scoping work depends on.

Purpose: MT-06 requires `DatabaseStorage.forTenant(tenantId)` as the instantiation mechanism. MT-08 requires the existing singleton to become `DatabaseStorage.forTenant(1)`. Until the class field and factory exist, no query method can reference `this.tenantId`.

Output: `server/storage.ts` with `private readonly tenantId: number`, `private constructor(tenantId: number)`, `static forTenant(tenantId: number): DatabaseStorage`, and `export const storage = DatabaseStorage.forTenant(1)` at line 2246.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-storage-refactor/39-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add private tenantId field, private constructor, and static forTenant factory to DatabaseStorage</name>
  <files>server/storage.ts</files>

  <read_first>
    - server/storage.ts (lines 414–460 — the class declaration and first fields; lines 2244–2246 — the current singleton export)
  </read_first>

  <action>
Make two precise edits to server/storage.ts:

**Edit 1 — Class opening (around line 414):**

Replace:
```typescript
export class DatabaseStorage implements IStorage {
  async getUsers(): Promise<User[]> {
```

With:
```typescript
export class DatabaseStorage implements IStorage {
  private readonly tenantId: number;

  private constructor(tenantId: number) {
    this.tenantId = tenantId;
  }

  static forTenant(tenantId: number): DatabaseStorage {
    return new DatabaseStorage(tenantId);
  }

  async getUsers(): Promise<User[]> {
```

Keep the existing `private chatSchemaEnsured = false`, `private companySchemaEnsured = false`, and `private conversationSchemaEnsured = false` fields exactly where they are — do NOT move or remove them.

**Edit 2 — Last line of file (line 2246):**

Replace:
```typescript
export const storage = new DatabaseStorage();
```

With:
```typescript
export const storage = DatabaseStorage.forTenant(1);
```

Do NOT touch any other code in the file. The query methods in the class body are NOT changed in this plan — they will be updated in plans 02 and 03.
  </action>

  <verify>
    <automated>cd /c/Users/Vanildo/Dev/skleanings && npm run check 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - server/storage.ts contains the line: `private readonly tenantId: number;`
    - server/storage.ts contains the line: `private constructor(tenantId: number) {`
    - server/storage.ts contains the line: `static forTenant(tenantId: number): DatabaseStorage {`
    - server/storage.ts contains the line: `return new DatabaseStorage(tenantId);` (inside forTenant, replacing the old public instantiation)
    - server/storage.ts last non-empty line is: `export const storage = DatabaseStorage.forTenant(1);`
    - The string `new DatabaseStorage()` (with empty parens) does NOT appear anywhere in server/storage.ts
    - `npm run check` exits with code 0 (TypeScript compiles without errors)
  </acceptance_criteria>

  <done>DatabaseStorage has the factory pattern in place. The existing singleton is `forTenant(1)`. TypeScript compile passes. Plans 02 and 03 can now reference `this.tenantId` in query methods.</done>
</task>

<task type="auto">
  <name>Task 2: Grep codebase for other new DatabaseStorage() instantiation sites</name>
  <files>server/storage.ts</files>

  <read_first>
    - server/storage.ts (lines 2244–2246 — verify the singleton was updated)
  </read_first>

  <action>
Run the following to confirm no other files instantiate DatabaseStorage directly (per Research Pitfall 6):

```bash
grep -rn "new DatabaseStorage" /c/Users/Vanildo/Dev/skleanings --include="*.ts" --include="*.tsx"
```

Expected output: zero matches (or only the single line inside the class's own `forTenant` method returning `new DatabaseStorage(tenantId)`).

If any file outside server/storage.ts contains `new DatabaseStorage()`, update that file to use `DatabaseStorage.forTenant(1)` instead.

If no external files contain `new DatabaseStorage()`, this task is complete with no changes needed beyond what Task 1 already did.
  </action>

  <verify>
    <automated>cd /c/Users/Vanildo/Dev/skleanings && grep -rn "new DatabaseStorage()" --include="*.ts" --include="*.tsx" . | grep -v "node_modules"</automated>
  </verify>

  <acceptance_criteria>
    - The grep command `grep -rn "new DatabaseStorage()" --include="*.ts"` returns zero results across the entire codebase (node_modules excluded)
    - The only `new DatabaseStorage` call is the one inside `static forTenant()` in server/storage.ts: `return new DatabaseStorage(tenantId)`
  </acceptance_criteria>

  <done>No external code instantiates DatabaseStorage directly. All instantiation flows through forTenant().</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `npm run check` exits code 0
2. `grep "private readonly tenantId" server/storage.ts` returns one match
3. `grep "static forTenant" server/storage.ts` returns one match
4. `grep "export const storage = DatabaseStorage.forTenant(1)" server/storage.ts` returns one match
5. `grep -rn "new DatabaseStorage()" --include="*.ts" .` returns zero matches (excluding node_modules)
</verification>

<success_criteria>
The class-level foundation for tenant scoping is in place. `DatabaseStorage.forTenant(N)` is the only public instantiation path. The `storage` singleton equals `DatabaseStorage.forTenant(1)`. TypeScript compiles without errors. Plans 02 and 03 can now add `this.tenantId` filters to query methods.
</success_criteria>

<output>
After completion, create `.planning/phases/39-storage-refactor/39-01-SUMMARY.md`
</output>
