---
phase: 40-tenant-resolution-middleware
plan: 03
type: execute
wave: 3
depends_on:
  - "40-01"
  - "40-02"
files_modified:
  - server/routes/auth-routes.ts
  - server/routes/availability.ts
  - server/routes/blog.ts
  - server/routes/bookings.ts
  - server/routes/calendar-sync.ts
  - server/routes/catalog.ts
  - server/routes/chat/index.ts
  - server/routes/client.ts
  - server/routes/company.ts
  - server/routes/contacts.ts
  - server/routes/faqs.ts
  - server/routes/integrations/ai.ts
  - server/routes/integrations/ghl.ts
  - server/routes/integrations/google-calendar.ts
  - server/routes/integrations/resend.ts
  - server/routes/integrations/stripe.ts
  - server/routes/integrations/telegram.ts
  - server/routes/integrations/twilio.ts
  - server/routes/notification-logs.ts
  - server/routes/payments.ts
  - server/routes/recurring-bookings.ts
  - server/routes/service-areas.ts
  - server/routes/staff.ts
  - server/routes/user-routes.ts
autonomous: true
requirements:
  - MT-12

must_haves:
  truths:
    - "No business route file imports the global storage singleton"
    - "Every route handler reads storage from res.locals.storage and passes it to lib function calls"
    - "npm run check passes with zero TypeScript errors"
  artifacts:
    - path: "server/routes/bookings.ts"
      provides: "Bookings route with tenant-scoped storage from res.locals"
      contains: "res.locals.storage"
    - path: "server/routes/catalog.ts"
      provides: "Catalog routes with tenant-scoped storage from res.locals"
      contains: "res.locals.storage"
    - path: "server/routes/staff.ts"
      provides: "Staff routes with tenant-scoped storage from res.locals"
      contains: "res.locals.storage"
  key_links:
    - from: "server/routes/bookings.ts"
      to: "server/lib/booking-ghl-sync.ts"
      via: "storage parameter passed to syncBookingToGhl"
      pattern: "syncBookingToGhl.*storage"
    - from: "server/routes/availability.ts"
      to: "server/lib/availability.ts"
      via: "storage parameter passed to getAvailabilityForDate"
      pattern: "getAvailabilityForDate.*storage"
    - from: "server/routes/company.ts"
      to: "server/lib/seo-injector.ts"
      via: "storage parameter passed to seo injector functions"
      pattern: "res.locals.storage"
---

<objective>
Migrate all 24 business route files from importing the global storage singleton to reading storage from res.locals.storage, and update all lib function call sites to pass storage as the first argument.

Purpose: This is the final MT-12 step — after the middleware (Plan 01) and lib refactor (Plan 02), route files are the last consumers of the global singleton. Removing the singleton import from route files completes tenant isolation.
Output: 24 route files with `import { storage }` removed and `const storage = res.locals.storage!` pattern at each handler
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-tenant-resolution-middleware/40-RESEARCH.md
@.planning/phases/40-tenant-resolution-middleware/40-01-SUMMARY.md
@.planning/phases/40-tenant-resolution-middleware/40-02-SUMMARY.md

@server/routes/bookings.ts
@server/routes/catalog.ts
@server/routes/availability.ts
@server/routes/staff.ts
@server/routes/company.ts
</context>

<interfaces>
<!-- Pattern to apply in every route handler -->

From Plan 02 (lib files now accept IStorage parameter):
```typescript
// availability.ts
export async function getAvailabilityForDate(storage: IStorage, date: string, ...): Promise<...>
export async function getAvailabilityRange(storage: IStorage, ...): Promise<...>

// booking-ghl-sync.ts
export async function syncBookingToGhl(storage: IStorage, booking: ...): Promise<...>

// notification-logger.ts
export async function logNotification(storage: IStorage, entry: InsertNotificationLog): Promise<void>

// staff-availability.ts
export async function getStaffAvailableSlots(storage: IStorage, ...): Promise<...>
```

Route handler migration pattern:
```typescript
// BEFORE
import { storage } from "../storage";
router.get("/bookings", async (req, res) => {
  const bookings = await storage.getBookings();
  const ghlResult = await syncBookingToGhl(booking);
});

// AFTER
// (no storage import at module level)
router.get("/bookings", async (req, res) => {
  const storage = res.locals.storage!;
  const bookings = await storage.getBookings();
  const ghlResult = await syncBookingToGhl(storage, booking);
});
```

Files NOT to migrate (keep global storage):
- server/routes/super-admin.ts — excluded (MT-13)
- server/routes/analytics.ts — excluded (uses server/storage/index.ts modular storage, not DatabaseStorage)
- server/services/*.ts — excluded (cron workers, no req/res context)
- server/lib/seeds.ts — excluded (startup seed, not per-request)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate high-traffic core routes (bookings, catalog, staff, availability, company)</name>
  <files>
    server/routes/bookings.ts,
    server/routes/catalog.ts,
    server/routes/staff.ts,
    server/routes/availability.ts,
    server/routes/company.ts
  </files>
  <read_first>
    - server/routes/bookings.ts (full file — 534 lines, 43 storage calls)
    - server/routes/catalog.ts (full file — 479 lines, 34 storage calls)
    - server/routes/staff.ts (full file — 388 lines, 26 storage calls)
    - server/routes/availability.ts (full file — 7 storage calls)
    - server/routes/company.ts (full file — 6 storage calls)
    - .planning/phases/40-tenant-resolution-middleware/40-02-SUMMARY.md (updated lib signatures)
  </read_first>
  <action>
    For each file, apply this migration:

    **Step 1 — Remove the module-level storage import:**
    Remove the line: `import { storage } from "../storage";`

    **Step 2 — Add res.locals.storage at each handler:**
    Inside each route handler function body (the `async (req, res) => { ... }` callback), add as the FIRST line:
    ```typescript
    const storage = res.locals.storage!;
    ```
    This shadows any outer `storage` variable and provides the tenant-scoped instance.

    **Step 3 — Update lib function call sites:**
    Wherever a lib function now requires a storage parameter (per Plan 02 changes), add storage as the first argument:
    - `getAvailabilityForDate(date, ...)` → `getAvailabilityForDate(storage, date, ...)`
    - `getAvailabilityRange(...)` → `getAvailabilityRange(storage, ...)`
    - `checkAvailability(...)` → `checkAvailability(storage, ...)`
    - `syncBookingToGhl(booking)` → `syncBookingToGhl(storage, booking)`
    - `syncBookingToClient(booking)` → `syncBookingToClient(storage, booking)`
    - `logNotification(entry)` → `logNotification(storage, entry)`
    - `getStaffAvailableSlots(...)` → `getStaffAvailableSlots(storage, ...)`
    - `getStaffUnionSlots(...)` → `getStaffUnionSlots(storage, ...)`
    - `getSlotsForServices(...)` → `getSlotsForServices(storage, ...)`
    - `acquireTimeSlotLock(...)` → `acquireTimeSlotLock(storage, ...)`
    - `releaseTimeSlotLock(...)` → `releaseTimeSlotLock(storage, ...)`
    - seo-injector functions that received storage: check 40-02-SUMMARY.md for exact signatures

    **bookings.ts specifics:** This file also imports `storage` for analytics attribution (`linkBookingToAttribution`, `recordConversionEvent`). These come from `"../storage/analytics"` (the modular analytics storage) — NOT from `"../storage"`. Leave the analytics import untouched; only remove `import { storage } from "../storage"`.

    **company.ts specifics:** May call seo-injector functions. Check the updated seo-injector signature from 40-02-SUMMARY.md and pass storage accordingly.

    Run `npm run check` after completing all 5 files.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import { storage }" /c/Users/Vanildo/Dev/skleanings/server/routes/bookings.ts` returns NO matches
    - `grep "import { storage }" /c/Users/Vanildo/Dev/skleanings/server/routes/catalog.ts` returns NO matches
    - `grep "import { storage }" /c/Users/Vanildo/Dev/skleanings/server/routes/staff.ts` returns NO matches
    - `grep "import { storage }" /c/Users/Vanildo/Dev/skleanings/server/routes/availability.ts` returns NO matches
    - `grep "import { storage }" /c/Users/Vanildo/Dev/skleanings/server/routes/company.ts` returns NO matches
    - `grep -c "res.locals.storage" /c/Users/Vanildo/Dev/skleanings/server/routes/bookings.ts` returns a number > 0
    - `grep -c "res.locals.storage" /c/Users/Vanildo/Dev/skleanings/server/routes/catalog.ts` returns a number > 0
    - `npm run check` passes (or only has errors from route files not yet migrated in this task — check output carefully)
  </acceptance_criteria>
  <done>5 high-traffic route files have no global storage import; each handler reads storage from res.locals.storage; lib calls pass storage as first argument; TypeScript clean for these files</done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining route files and integration routes</name>
  <files>
    server/routes/auth-routes.ts,
    server/routes/blog.ts,
    server/routes/calendar-sync.ts,
    server/routes/chat/index.ts,
    server/routes/client.ts,
    server/routes/contacts.ts,
    server/routes/faqs.ts,
    server/routes/integrations/ai.ts,
    server/routes/integrations/ghl.ts,
    server/routes/integrations/google-calendar.ts,
    server/routes/integrations/resend.ts,
    server/routes/integrations/stripe.ts,
    server/routes/integrations/telegram.ts,
    server/routes/integrations/twilio.ts,
    server/routes/notification-logs.ts,
    server/routes/payments.ts,
    server/routes/recurring-bookings.ts,
    server/routes/service-areas.ts,
    server/routes/user-routes.ts
  </files>
  <read_first>
    - server/routes/chat/index.ts (full file — has DI pattern, needs special handling)
    - server/routes/blog.ts (full file — 18 storage calls)
    - server/routes/integrations/ai.ts (full file — 18 storage calls)
    - server/routes/integrations/ghl.ts (full file — 12 storage calls)
    - server/routes/payments.ts (full file — 9 storage calls)
    - server/routes/recurring-bookings.ts (full file — 7 storage calls)
    - .planning/phases/40-tenant-resolution-middleware/40-02-SUMMARY.md (updated lib signatures)
  </read_first>
  <action>
    Apply the same migration pattern as Task 1 to all 19 remaining route files:
    1. Remove `import { storage } from "../storage";` (or `"../../storage"` for integrations/)
    2. Add `const storage = res.locals.storage!;` as the first line of each route handler
    3. Update lib function calls to pass storage as first argument (where lib signatures were updated in Plan 02)

    **Integration routes** use `"../../storage"` relative path — adjust accordingly. Same pattern applies.

    **server/routes/chat/index.ts special handling:**
    The chat module has a DI container (`setChatDependencies`). The file likely calls `setChatDependencies({ storage })` at module initialization time. This must change: do NOT call `setChatDependencies` at module level with the global singleton. Instead, call it inside each handler with `res.locals.storage!`:
    ```typescript
    router.post("/chat", async (req, res) => {
      const storage = res.locals.storage!;
      setChatDependencies({ storage });
      // rest of handler
    });
    ```
    Read the full chat/index.ts to understand the actual pattern and apply the correct fix.

    **server/routes/email-resend, google-calendar, stripe, telegram, twilio** — these integration routes call lib functions. Check which lib functions were updated in Plan 02 (40-02-SUMMARY.md) and pass storage to those calls.

    After migrating all 19 files, run `npm run check`. This should now be fully clean (all lib and route files updated).

    If any TypeScript errors remain, fix them before marking done. Common issues:
    - `res.locals.storage` is `IStorage | undefined` — use non-null assertion `!` or add a guard
    - Lib function called without storage argument — add storage as first arg
    - Circular type inference — use explicit type annotation
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "import { storage }" /c/Users/Vanildo/Dev/skleanings/server/routes/` returns ONLY matches from `super-admin.ts` and `analytics.ts` (the two excluded files)
    - `grep -rn "res.locals.storage" /c/Users/Vanildo/Dev/skleanings/server/routes/blog.ts` returns matches
    - `grep -rn "res.locals.storage" /c/Users/Vanildo/Dev/skleanings/server/routes/integrations/ai.ts` returns matches
    - `grep -rn "res.locals.storage" /c/Users/Vanildo/Dev/skleanings/server/routes/chat/index.ts` returns matches
    - `grep -rn "res.locals.storage" /c/Users/Vanildo/Dev/skleanings/server/routes/payments.ts` returns matches
    - `npm run check` passes with ZERO TypeScript errors
  </acceptance_criteria>
  <done>All 24 business route files have no global storage singleton import; every handler reads storage from res.locals.storage; lib calls pass storage as first argument; npm run check passes clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete tenant resolution middleware stack:
    - Plan 01: resolveTenantMiddleware with LRU cache (max 500, 5-min TTL), locals.d.ts, routes.ts ordering
    - Plan 02: 11 lib files refactored to accept IStorage parameter
    - Plan 03: 24 route files migrated to res.locals.storage
  </what-built>
  <how-to-verify>
    Start the dev server: `npm run dev`

    Run these curl tests (the server must be running on port 5000):

    **Test 1 — Unknown hostname returns 404 (MT-09):**
    ```bash
    curl -s -o /dev/null -w "%{http_code}" -H "Host: unknown.xkedule.com" http://localhost:5000/api/categories
    ```
    Expected: 404

    **Test 2 — Known hostname (localhost) resolves correctly (MT-11):**
    ```bash
    curl -s -H "Host: localhost" http://localhost:5000/api/categories
    ```
    Expected: 200 with JSON categories array (not 404)

    **Test 3 — Super-admin bypass works (MT-13):**
    ```bash
    curl -s -o /dev/null -w "%{http_code}" -H "Host: unknown.xkedule.com" http://localhost:5000/api/super-admin/login
    ```
    Expected: NOT 404 (should be 400 or 405 — the route handler runs, meaning the middleware was bypassed)

    **Test 4 — LRU cache active (MT-10):**
    Make two sequential requests with the same hostname and verify the app does not log two DB queries (add a temporary `console.log("DB LOOKUP: ", hostname)` in tenant.ts before the cache check, make two requests, observe that the log appears only once, then remove the log).
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 tests pass, or describe which test failed and what the actual response was</resume-signal>
</task>

</tasks>

<verification>
Final gate — after human approval:

1. `grep -rn "import { storage }" /c/Users/Vanildo/Dev/skleanings/server/routes/` shows only super-admin.ts and analytics.ts
2. `grep -rn "import { storage }" /c/Users/Vanildo/Dev/skleanings/server/lib/` shows only seeds.ts
3. `npm run check` returns 0 errors
4. All 5 phase success criteria verified manually:
   - Unknown hostname → 404
   - Second request served from LRU cache (no DB hit)
   - res.locals.storage used in all business routes
   - /api/super-admin/* unrestricted
   - res.locals.tenant populated on resolved requests
</verification>

<success_criteria>
- `import { storage }` absent from all business route files (super-admin.ts and analytics.ts exempted)
- `import { storage }` absent from all lib files (seeds.ts exempted)
- `res.locals.storage` present in every migrated route handler
- `npm run check` passes with zero errors
- Human smoke tests confirm all 5 phase success criteria
</success_criteria>

<output>
After completion, create `.planning/phases/40-tenant-resolution-middleware/40-03-SUMMARY.md`
</output>
